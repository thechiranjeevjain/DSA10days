# Master DSA Reconstruction + Interview Articulation Table

Purpose: one continuous sharpening table for reconstructing the engine and speaking the exact correctness contract before coding. This is not a solution summary.

Organization: Pattern -> Sub-pattern divider rows live inside one uninterrupted table. Similar problems stay together in anchor-to-mutation progression; use `01_ZERO_TO_HERO_RANKED_TABLE.md` when ROI rank order is the goal.

Each visible problem name opens its local Java source. Read: family skeleton -> problem skeleton -> state/invariant -> exact transition -> trap -> smallest useful mutation.

| Problem | Reconstruction + Correctness Contract | Trap / Mutation |
|---|---|---|
| **HashMap / Frequency / Set -> Two pointers / hash** | **Family skeleton:** scan current -> query prior count/complement/ownership -> consume or record exactly once -> keep map meaning true | |
| [Two Sum](../../src/main/java/org/chijai/day1/Arrays/session2/Three3Sum2Sum.java) | **Skeleton:** Scan left to right, if target - nums[i] exists return indices, otherwise store nums[i] -> i. **Contract:** The map contains only indices already passed, so a complement hit never reuses the current element. For current value x, ask whether target - x has been seen before inserting x. If found, return the stored old index and the current index. | **Trap:** check complement before insert; no self reuse **Mutation:** Sort the input and ask for the pair values rather than original indices. -> Two Pointers: Sum comparison now proves which endpoint can be discarded. |
| **HashMap / Frequency / Set -> Frequency count** | **Family skeleton:** scan current -> query prior count/complement/ownership -> consume or record exactly once -> keep map meaning true | |
| [Valid Anagram](../../src/main/java/org/chijai/day3/session3/ValidAnagram.java) | **Skeleton:** Reject different lengths, increment for s and decrement for t, then verify all counts zero. **Contract:** The count array/map is the net balance between the two strings. Increment for one string and decrement for the other, then every count must be zero. If lengths differ, fail before counting. | **Trap:** reject unequal lengths; every net count must finish at zero **Mutation:** Ask whether any fixed-length substring of s is an anagram of p. -> Sliding Window: The same frequency balance must now follow a moving contiguous region. |
| **HashMap / Frequency / Set -> Boyer-Moore / frequency** | **Family skeleton:** scan current -> query prior count/complement/ownership -> consume or record exactly once -> keep map meaning true | |
| [Majority Element](../../src/main/java/org/chijai/day1/Arrays/session2/MajorityElement.java) | **Skeleton:** Track candidate and count; reset at zero, increment on match, decrement otherwise. **Contract:** candidate survives pair cancellation between different values. When count becomes zero, the current value becomes the new candidate; equal increments and different decrements. This is valid because a value appearing more than n/2 cannot be fully cancelled. | **Trap:** reset candidate only when count == 0 **Mutation:** Ask for the k most frequent values instead of the one value occurring over n/2. -> Heap: The majority cancellation guarantee disappears and bounded ranking becomes necessary. |
| **HashMap / Frequency / Set -> HashMap/frequency** | **Family skeleton:** scan current -> query prior count/complement/ownership -> consume or record exactly once -> keep map meaning true | |
| [Ransom Note](../../src/main/java/org/chijai/day1/Arrays/session1/RansomNote.java) | **Skeleton:** Build int[26] or map from magazine, decrement while scanning ransomNote, return false below zero. **Contract:** Count magazine chars, then spend counts for ransom; fail when a needed char is missing. | **Trap:** fail on the first negative remaining count; do not reuse a magazine character **Mutation:** Ask for the shortest contiguous magazine substring that supplies the ransom characters. -> Sliding Window: Need counts must be maintained while shrinking one valid region. |
| **HashMap / Frequency / Set -> Hash/frequency** | **Family skeleton:** scan current -> query prior count/complement/ownership -> consume or record exactly once -> keep map meaning true | |
| [Longest Palindrome](../../src/main/java/org/chijai/day3/session3/LongestPalindrome.java) | **Skeleton:** Count chars, add count / 2 * 2, and allow one odd center if any count is odd. **Contract:** At most one character may have an odd count; pairs from all counts build the longest palindrome. | **Trap:** use every pair, but allow at most one odd center **Mutation:** Require the chosen characters to be contiguous in the original string. -> Expand Around Center: Global frequency pairs no longer prove substring placement. |
| **Binary Search / Answer Search -> Binary search invariant** | **Family skeleton:** define inclusive search boundary -> test mid -> discard only the proven-impossible side -> preserve and return the required boundary | |
| [Binary Search](../../src/main/java/org/chijai/day2/session1/BinarySearch.java) | **Skeleton:** While left <= right, compare nums[mid] to target; move left/right, return index or -1. **Contract:** The target can only be inside the current inclusive range [left, right]. Compare nums[mid] with target, then discard the half that sorted order proves impossible. Stop when left > right, because no candidate index remains. | **Trap:** left <= right; move by mid +/- 1 **Mutation:** Allow duplicates and ask for the first target occurrence. -> Boundary Binary Search: A hit must be preserved while the search continues left. |
| [Search Insert Position](../../src/main/java/org/chijai/day2/session1/BinarySearch.java) | **Skeleton:** Binary search with answer initialized to n; when nums[mid] >= target save mid and move right left. **Contract:** Find the first index whose value is >= target; if none, insert at n. | **Trap:** return the first >= target boundary, including n when no such index exists **Mutation:** Ask only whether target exists, not where it should be inserted. -> Exact Binary Search: Equality can return immediately; no lower-bound answer must be preserved. |
| [First Bad Version](../../src/main/java/org/chijai/day2/session1/BinarySearch.java) | **Skeleton:** If isBadVersion(mid), save mid and search left; otherwise search right. **Contract:** Find the first true in a false...false,true...true version predicate. | **Trap:** preserve mid when it is bad; search only versions 1 through n **Mutation:** Permit good versions after a bad version. -> Linear Scan: The predicate is no longer monotonic, so discarding half is unsound. |
| **Binary Search / Answer Search -> Binary search on answer** | **Family skeleton:** search candidate answer -> feasible(mid)? save and move toward better : move toward feasible region | |
| [Koko Eating Bananas](../../src/main/java/org/chijai/day2/session2/KokoBananas.java) | **Skeleton:** Search speed 1..maxPile, compute total ceil(pile/speed) hours, keep smaller working speed. **Contract:** speed is the candidate bananas per hour and must start at 1, never 0. For each pile, required hours are ceil(pile / speed), and feasibility is totalHours <= h. If a speed works, every higher speed works, so save it and search smaller. | **Trap:** speed starts at 1; ceil division **Mutation:** Fix the candidate speed/capacity/day and ask only whether it is feasible. -> Greedy Linear Scan: The outer monotonic answer search disappears and only its proof checker remains. |
| [Split Array Largest Sum](../../src/main/java/org/chijai/day2/session2/AGGRCOW.java) | **Skeleton:** Search max(nums)..sum(nums), greedily count pieces when current sum would exceed mid. **Contract:** currentSum is the sum of the current contiguous partition and must never exceed maxAllowedSum. If currentSum + x > maxAllowedSum, close the current partition and let x start the next one. Feasibility is pieces <= m because extra allowed partitions can be split later when needed. | **Trap:** > not >=; x starts new partition **Mutation:** Fix the candidate speed/capacity/day and ask only whether it is feasible. -> Greedy Linear Scan: The outer monotonic answer search disappears and only its proof checker remains. |
| [Capacity To Ship Packages Within D Days](../../src/main/java/org/chijai/day2/session2/KokoBananas.java) | **Skeleton:** Search maxWeight..sumWeight, count days by accumulating load until capacity would overflow. **Contract:** currentLoad is the load already assigned to the current day and must never exceed capacity. Before assigning w, if currentLoad + w > capacity, open a new day and let w start that day; equality is allowed in the current day. Feasibility is requiredDays <= days. | **Trap:** > not >=; w starts new day **Mutation:** Fix the candidate speed/capacity/day and ask only whether it is feasible. -> Greedy Linear Scan: The outer monotonic answer search disappears and only its proof checker remains. |
| [Minimum Number Of Days To Make M Bouquets](../../src/main/java/org/chijai/day2/session2/KokoBananas.java) | **Skeleton:** Reject if m*k > n; for each day mid, count adjacent bloomed streaks of length k. **Contract:** flowers counts consecutive bloomed flowers not yet consumed into a bouquet. A flower with bloomDay <= day extends the streak; an unbloomed flower breaks adjacency and resets flowers to 0. When flowers == k, one bouquet consumes those k flowers, so increment bouquets and reset flowers to 0. | **Trap:** two resets: gap and flowers == k **Mutation:** Fix the candidate speed/capacity/day and ask only whether it is feasible. -> Greedy Linear Scan: The outer monotonic answer search disappears and only its proof checker remains. |
| **Binary Search / Answer Search -> Binary search boundary** | **Family skeleton:** define inclusive search boundary -> test mid -> discard only the proven-impossible side -> preserve and return the required boundary | |
| [Search In Rotated Sorted Array](../../src/main/java/org/chijai/day2/session1/SearchRange.java) | **Skeleton:** Compare nums[left] and nums[mid] to identify sorted half, then discard the half that cannot contain target. **Contract:** At least one half around mid is sorted. Decide which half is sorted, then keep it only if target lies within that half's inclusive bounds. Otherwise discard it and search the other half. | **Trap:** identify the sorted half before testing whether target lies inside it **Mutation:** Allow arbitrary unsorted order while keeping one target lookup. -> HashMap: Sorted-half elimination disappears, so direct value-to-index lookup becomes the reusable state. |
| [Find First And Last Position Of Element In Sorted Array](../../src/main/java/org/chijai/day2/session1/SearchRange.java) | **Skeleton:** findFirst moves left on nums[mid] >= target; findLast moves right on nums[mid] <= target. **Contract:** Use two boundary searches, not one hit plus expansion. The left boundary is the first index with value >= target; the right boundary is the last index with value <= target. Verify the boundaries actually equal target before returning them. | **Trap:** two boundary searches; verify equality **Mutation:** Remove sorted order and ask for the first and last target positions. -> Linear Scan: Order no longer supports boundary elimination. |
| [Search In Rotated Sorted Array II](../../src/main/java/org/chijai/day2/session1/SearchRange.java) | **Skeleton:** If nums[left]==nums[mid]==nums[right], left++ and right--; otherwise reuse sorted-half logic. **Contract:** With duplicates, shrink both ends only when left, mid, and right are equal and ordering is ambiguous. | **Trap:** when left, mid, and right are equal, shrink both ends before choosing a half **Mutation:** Require the first matching index among duplicates. -> Boundary-Aware Linear/Binary Hybrid: Duplicate ambiguity now affects both half choice and answer ownership. |
| [Find Peak Element](../../src/main/java/org/chijai/day2/session1/SearchRange.java) | **Skeleton:** If nums[mid] > nums[mid+1], move right to mid; else move left to mid+1 until left == right. **Contract:** Compare mid with mid+1; the rising side must contain a peak. | **Trap:** use left < right so mid + 1 stays valid; keep mid on a descending slope **Mutation:** Ask for every peak index instead of any one peak. -> Linear Scan: Binary search can discard peaks that the output now requires. |
| [Sqrtx](../../src/main/java/org/chijai/day2/session1/SearchRange.java) | **Skeleton:** Binary search 0..x, cast mid*mid to long, save mid when square <= x. **Contract:** Find the largest integer mid whose square is <= x. | **Trap:** avoid mid * mid overflow; return the floor boundary, not the final failed mid **Mutation:** Ask for the integer kth root rather than square root. -> Binary Search on Answer: Replace square feasibility with overflow-safe power comparison. |
| **Binary Search / Answer Search -> HashMap + binary search** | **Family skeleton:** define monotonic candidate space -> test mid -> preserve feasible boundary -> discard half -> return first/last required candidate | |
| [Time Based Key Value Store](../../src/main/java/org/chijai/day2/session3/TimeBasedKeyValueStore.java) | **Skeleton:** Append on set; on get binary search the key's list for rightmost timestamp <= target. **Contract:** Map each key to timestamped values in order; binary search finds latest timestamp <= query. | **Trap:** return the last timestamp <= query; preserve each key's timestamp order **Mutation:** Allow set calls to arrive out of timestamp order. -> Ordered Map: Append-only arrays lose sorted order, so each key needs ordered insertion and floor lookup. |
| **Sliding Window -> Sliding window / set** | **Family skeleton:** expand right + add state -> while invalid or shrinkable, remove left + advance -> record answer at the valid moment | |
| [Longest Substring Without Repeating Characters](../../src/main/java/org/chijai/day3/session1/LongestSubString.java) | **Skeleton:** Expand right, while duplicate exists remove left, then update max. **Contract:** The active window contains no duplicate characters. When the current character was last seen inside the window, move left to lastSeen[ch] + 1, not one step blindly. Record the answer after the window is valid. | **Trap:** left = max(left, lastSeen + 1) **Mutation:** Ask only how many distinct characters occur in the whole string. -> HashSet: Contiguous boundaries disappear and global membership is sufficient. |
| **Sliding Window -> Sliding window / need-have** | **Family skeleton:** expand right + add state -> while invalid or shrinkable, remove left + advance -> record answer at the valid moment | |
| [Minimum Window Substring](../../src/main/java/org/chijai/day3/session1/MinimumWindowSubstring.java) | **Skeleton:** Build need map, update have on right, while have == needCount update best and remove left. **Contract:** window counts only the current [left, right] characters and formed/have counts how many required character quotas are satisfied. Expand until every required quota is covered, then shrink from left while still valid and save the best before breaking validity. Extra copies do not increase formed once the quota is already met. | **Trap:** save answer before left removal breaks validity **Mutation:** Require t as an ordered subsequence rather than an unordered multiset. -> Dynamic Programming / Two-Pass Subsequence Scan: Need/have frequency validity no longer captures order. |
| **Sliding Window -> Sliding window** | **Family skeleton:** expand right + add state -> while invalid or shrinkable, remove left + advance -> record answer at the valid moment | |
| [Longest Substring With At Most K Distinct Characters](../../src/main/java/org/chijai/day3/session1/AtMostKDistinct.java) | **Skeleton:** Expand right count, while distinct > k decrement/remove left, update max length. **Contract:** Keep a frequency map with at most k distinct chars; shrink until valid. | **Trap:** shrink while distinct > k and erase a character only when its count reaches zero **Mutation:** Ask for the number of substrings with exactly k distinct characters. -> Two At-Most Windows: Longest-window tracking becomes atMost(k) minus atMost(k-1) counting. |
| **Sliding Window -> Sliding window frequency** | **Family skeleton:** expand right + add state -> while invalid or shrinkable, remove left + advance -> record answer at the valid moment | |
| [Find All Anagrams In A String](../../src/main/java/org/chijai/day3/session1/FindAllAnagramsInAString.java) | **Skeleton:** Maintain difference counts or match count across a window of length p. **Contract:** The window length must stay exactly p.length. Add the right char, remove the char that falls out once the window is too large, then compare frequency state. Record the left index only for complete matching windows. | **Trap:** keep exactly p.length characters; compare only after outgoing state is removed **Mutation:** Ask only whether the two complete strings are anagrams. -> Frequency Count: Fixed moving boundaries are no longer part of the workload. |
| **Sliding Window -> Queue / stream** | **Family skeleton:** expand right + add state -> while invalid or shrinkable, remove left + advance -> record answer at the valid moment | |
| [Moving Average From Data Stream](../../src/main/java/org/chijai/day7/session1/heap/MovingAverage.java) | **Skeleton:** Offer val, add to sum, if queue too large poll and subtract, return sum/count. **Contract:** Queue last size values and running sum; average is sum divided by queue size. | **Trap:** evict only after size exceeds capacity; divide by current size during warm-up **Mutation:** Change stream calls into many fixed-range average queries on an array. -> Prefix/Suffix: Prefix sums answer each range in O(1). |
| **Sliding Window -> Prefix/window counting** | **Family skeleton:** expand right + add state -> while invalid or shrinkable, remove left + advance -> record answer at the valid moment | |
| [Count Number Of Nice Subarrays](../../src/main/java/org/chijai/day3/session2/prefix/suffix/NiceSubArrays.java) | **Skeleton:** Count subarrays with at most k odd numbers using a sliding left pointer, subtract atMost(k-1). **Contract:** Exactly k odds equals atMost(k) minus atMost(k-1), or prefix count of odd count. | **Trap:** exactly k requires atMost(k) - atMost(k - 1); handle k = 0 **Mutation:** Allow arbitrary positive and negative values and ask for exact subarray sum k. -> Prefix Sum + HashMap: Removing left no longer changes the sum monotonically. |
| **Sliding Window -> DP / patience sorting** | **Family skeleton:** expand right + add state -> while invalid or shrinkable, remove left + advance -> record answer at the valid moment | |
| [Longest Continuous Increasing Subsequence](../../src/main/java/org/chijai/day9/dp/session2/LIS.java) | **Skeleton:** Scan once, current = nums[i] > nums[i-1] ? current + 1 : 1, update best. **Contract:** Continuous means subarray, so reset the current streak whenever nums[i] <= nums[i-1]. | **Trap:** continuous forbids skips; reset the streak on nums[i] <= nums[i - 1] **Mutation:** Allow deleting elements while preserving order. -> LIS / DP: Each index can extend many earlier smaller indices. |
| **Prefix Sum / Prefix-Suffix -> Prefix/suffix** | **Family skeleton:** seed empty prefix/suffix -> accumulate reusable state -> combine/query strict boundaries without including the wrong element | |
| [Product Of Array Except Self](../../src/main/java/org/chijai/day3/session2/prefix/suffix/ProductOfArrayExceptSelf.java) | **Skeleton:** Fill answer with left products, then multiply by running right product from the end. **Contract:** answer[i] first receives the product of all values strictly left of i, then gets multiplied by the product strictly right of i. The current element is never included in its own answer. This avoids division and handles zeros naturally. | **Trap:** each pass must exclude nums[i]; do not divide because zeros are legal **Mutation:** Ask many immutable range-product queries instead of one except-self array. -> Prefix Products: Precomputed cumulative products answer repeated interval workload when division/zero policy permits. |
| **Prefix Sum / Prefix-Suffix -> Prefix/window counting** | **Family skeleton:** seed empty prefix/suffix -> accumulate reusable state -> combine/query strict boundaries without including the wrong element | |
| [Binary Subarrays With Sum](../../src/main/java/org/chijai/day3/session2/prefix/suffix/NiceSubArrays.java) | **Skeleton:** Implement atMost(sum): expand right, shrink while sum > goal, add window length. **Contract:** For binary nonnegative arrays, exact goal equals atMost(goal) - atMost(goal - 1). atMost keeps a valid window with sum <= goal and adds right - left + 1 subarrays ending at right. Guard goal < 0 as zero. | **Trap:** atMost(goal) - atMost(goal - 1) **Mutation:** Allow arbitrary integers while keeping exact-sum subarray counting. -> Prefix Sum + HashMap: The at-most window identity depends on nonnegative values and now breaks. |
| **Linked List Pointers -> Pointer reversal** | **Family skeleton:** name pointer ownership -> save the next structure -> rewire current group -> reconnect both boundaries -> return the real head | |
| [Reverse Linked List](../../src/main/java/org/chijai/day4/LinkedList/session1/ReverseLinkedList.java) | **Skeleton:** Keep prev, curr, next; curr.next = prev; advance; return prev. **Contract:** Save next before changing current.next. After rewiring current.next to prev, move prev to current and current to saved next. When current becomes null, prev is the new head. | **Trap:** save next before current.next rewrite **Mutation:** Forbid in-place mutation and ask for values in reverse order. -> Stack: LIFO output replaces pointer rewiring. |
| **Linked List Pointers -> Fast/slow pointers** | **Family skeleton:** move slow by 1 and fast by 2 -> use where fast stops or where pointers meet -> preserve null guards before every 2-step move | |
| [Linked List Cycle](../../src/main/java/org/chijai/day4/LinkedList/session1/LinkedListCycle.java) | **Skeleton:** Move slow one, fast two while fast and fast.next exist; meeting means cycle. **Contract:** slow advances one node and fast advances two nodes. Without a cycle, fast or fast.next reaches null; inside a cycle, the speed difference forces a meeting. Never dereference fast.next before checking it. | **Trap:** guard fast and fast.next **Mutation:** Allow O(n) extra memory and ask only whether a node repeats. -> HashSet: Identity membership detects the first revisit without speed-distance reasoning. |
| [Middle Of Linked List](../../src/main/java/org/chijai/day4/LinkedList/session4/MiddleOfLinkedList.java) | **Skeleton:** Start slow and fast at head; while fast != null and fast.next != null, move slow once and fast twice; return slow. **Contract:** slow is the middle candidate and fast has covered twice as many edges; when fast cannot move two steps, slow is the required second middle. | **Trap:** even length returns second middle; guard fast.next **Mutation:** Ask repeated random index queries on the same immutable list. -> Array Indexing: One materialization amortizes traversal across many queries. |
| **Linked List Pointers -> Merge / dummy node** | **Family skeleton:** name node ownership -> save next before mutation -> rewire/advance -> reconnect changed boundaries -> return actual head | |
| [Merge Two Sorted Lists](../../src/main/java/org/chijai/day4/LinkedList/session4/Merge2SortedLists.java) | **Skeleton:** Compare l1/l2, append smaller to tail, advance, then attach remainder. **Contract:** dummy.next is the real head and tail is the last node already attached to the merged list. At each step attach the smaller current node and advance only that source list. After one list ends, attach the remaining suffix directly. | **Trap:** advance only the list whose node was attached; append the remaining suffix **Mutation:** Increase the input from two sorted lists to k sorted lists. -> Min-Heap: The next node must be selected among k current heads. |
| **Linked List Pointers -> HashMap + doubly linked list** | **Family skeleton:** name node ownership -> save next before mutation -> rewire/advance -> reconnect changed boundaries -> return actual head | |
| [LRU Cache](../../src/main/java/org/chijai/day4/LinkedList/session3/LruCache.java) | **Skeleton:** On get/put move node to front; if over capacity remove tail and map entry. **Contract:** The map owns key to node lookup, and the doubly linked list owns recency order from most-recent to least-recent. Every get or updated put moves the node to the front. When capacity is exceeded, remove the tail node from both list and map. | **Trap:** remove evicted node from map and list **Mutation:** Remove eviction and ask for plain get/put. -> HashMap/HashSet: Direct key lookup is enough. |
| [Design Browser History](../../src/main/java/org/chijai/day4/LinkedList/session3/LruCache.java) | **Skeleton:** Maintain current node; visit creates current.next and clears forward branch. **Contract:** Back/forward are pointer moves over a history chain; visit drops forward history. | **Trap:** visiting a new URL discards forward history; clamp back/forward at both ends **Mutation:** Retain every forward branch after visiting from the middle. -> Tree: History is no longer one chain; each page can own multiple future branches. |
| **Linked List Pointers -> HashMap / interleaving copy** | **Family skeleton:** name node ownership -> save next before mutation -> rewire/advance -> reconnect changed boundaries -> return actual head | |
| [Copy List With Random Pointer](../../src/main/java/org/chijai/day4/LinkedList/session2/CopyListWithRandomPointer.java) | **Skeleton:** First create clones in map, second assign clone.next and clone.random from mapped nodes. **Contract:** Each original node must map to exactly one cloned node, preserving identity rather than value. After clones exist, set each clone's next and random by looking up the original node's targets. Null random remains null. | **Trap:** clone by node identity, preserve null random, and restore the original list if interleaving **Mutation:** Remove random pointers and require an in-place structural copy. -> Linked List Pointers: Identity mapping is unnecessary when only next edges remain. |
| **Linked List Pointers -> Linked list two pointers** | **Family skeleton:** name node ownership -> save next before mutation -> rewire/advance -> reconnect changed boundaries -> return actual head | |
| [Intersection of Two Linked Lists](../../src/main/java/org/chijai/day4/LinkedList/session1/Intersection.java) | **Skeleton:** Move a and b one step; when null redirect to other head; return when a == b. **Contract:** Each pointer walks its list then switches to the other head at null. After both switches, the remaining path lengths are equalized. They meet at the shared node identity or both reach null. | **Trap:** compare node identity, not value **Mutation:** Allow O(n) extra memory and return the first shared node. -> HashSet: Stored node identity replaces path-length equalization. |
| [Middle of the Linked List](../../src/main/java/org/chijai/day4/LinkedList/session1/Intersection.java) | **Skeleton:** While fast and fast.next exist, move slow one and fast two, return slow. **Contract:** Fast moves twice as fast; slow lands at the middle when fast finishes. | **Trap:** lost next pointer or wrong returned head **Mutation:** Ask only whether a node repeats/intersects, allowing extra memory. -> HashMap/HashSet: Identity lookup is enough and avoids pointer algebra. |
| **Linked List Pointers -> Floyd cycle entry** | **Family skeleton:** move slow by 1 and fast by 2 -> use where fast stops or where pointers meet -> preserve null guards before every 2-step move | |
| [Linked List Cycle II](../../src/main/java/org/chijai/day4/LinkedList/session4/LinkedListCycleII.java) | **Skeleton:** Detect meeting, reset one pointer to head, move both until equal. **Contract:** After slow and fast meet inside the cycle, reset one pointer to head. Move both one step at a time; their meeting point is the cycle entry. The first meeting itself is not necessarily the entry. | **Trap:** meeting point is not always entry **Mutation:** Ask only whether a cycle exists, not its entry. -> Floyd Detection: The second head-to-meeting phase is no longer required. |
| **Linked List Pointers -> Linked-list reversal groups** | **Family skeleton:** name pointer ownership -> save the next structure -> rewire current group -> reconnect both boundaries -> return the real head | |
| [Reverse Nodes in k-Group](../../src/main/java/org/chijai/day4/LinkedList/session2/ReverseLinkedListNodesK.java) | **Skeleton:** Use dummy/groupPrev, locate kth, reverse group, reconnect, advance groupPrev. **Contract:** Before reversing, verify that the next k nodes exist. Reverse exactly that closed group, reconnect previous group tail to the new head, and connect the reversed tail to the next group. Leave a final short group unchanged. | **Trap:** confirm k nodes before reversing **Mutation:** Ask to reverse only node values while preserving links. -> Stack: Group values can be emitted/popped without structural reconnection. |
| [Odd Even Linked List](../../src/main/java/org/chijai/day4/LinkedList/session2/ReverseLinkedListNodesK.java) | **Skeleton:** Move odd to even.next and even to odd.next until even chain ends, then connect. **Contract:** Keep odd and even chains separately, then attach even head after odd tail. | **Trap:** save evenHead and reconnect odd tail to it after both chains are built **Mutation:** Partition nodes stably by an arbitrary predicate rather than index parity. -> Two Dummy Lists: Predicate ownership replaces implicit odd/even pointer cadence. |
| [Rotate List](../../src/main/java/org/chijai/day4/LinkedList/session2/ReverseLinkedListNodesK.java) | **Skeleton:** Count length and tail, connect tail to head, move to new tail, break circle. **Contract:** Make the list circular, then break at length - k % length. | **Trap:** reduce k modulo length; break the ring at length - k **Mutation:** Store the sequence in an array instead of a linked list. -> Reversal / Modular Indexing: Random access removes the need to form and break a cycle. |
| [Swap Nodes In Pairs](../../src/main/java/org/chijai/day4/LinkedList/session2/ReverseLinkedListNodesK.java) | **Skeleton:** For each pair, rewire prev->second, first->second.next, second->first. **Contract:** Dummy node lets you swap each adjacent pair without special-casing head. | **Trap:** preserve the next pair head and return dummy.next after rewiring **Mutation:** Swap adjacent values rather than nodes. -> Linear Scan: Pointer topology remains unchanged, so pairwise value exchange is sufficient. |
| [Reverse Linked List II](../../src/main/java/org/chijai/day4/LinkedList/session2/ReverseLinkedListNodesK.java) | **Skeleton:** Find node before left, then head-insert nodes from the sublist for right-left steps. **Contract:** Use a dummy and reverse exactly the sublist between left and right. | **Trap:** use a dummy before left and reconnect both the prefix and suffix **Mutation:** Make the segment immutable and request its reversed values. -> Stack: The bounded segment can be buffered without reconnecting links. |
| **Two Pointers -> Two pointers** | **Family skeleton:** place boundaries -> evaluate current pair/span -> move only the boundary whose movement safely discards candidates -> preserve best/validity | |
| [Valid Palindrome](../../src/main/java/org/chijai/day3/session3/ValidPalindrome.java) | **Skeleton:** Advance left/right past invalid chars, compare lowercase chars, stop when pointers cross. **Contract:** left and right move inward over the original string, skipping only non-alphanumeric characters. Compare normalized characters after both skips. A mismatch returns false; crossing pointers means every required pair matched. | **Trap:** skip non-alphanumerics on both sides before comparing normalized characters **Mutation:** Allow deleting at most one mismatching character. -> Branching Two Pointers: The first mismatch creates exactly two remaining ranges to validate. |
| [Container With Most Water](../../src/main/java/org/chijai/day1/Arrays/session2/ContainerWithMostWater.java) | **Skeleton:** Compute area at left/right, update max, move pointer with smaller height. **Contract:** Area is width times the shorter wall, so the shorter side is the limiting boundary. Moving the taller side cannot improve the old limiting height with smaller width. Move the shorter side and keep the best area seen. | **Trap:** move the shorter wall; moving only the taller wall cannot improve the limiting height **Mutation:** Ask for the k largest container areas, not only the maximum. -> Heap + Pair Enumeration: The single dominance move cannot preserve every top pair. |
| **Two Pointers -> Two pointers / hash** | **Family skeleton:** place boundaries -> evaluate current pair/span -> move only the boundary whose movement safely discards candidates -> preserve best/validity | |
| [Two Sum II - Input Array Is Sorted](../../src/main/java/org/chijai/day1/Arrays/session2/Three3Sum2Sum.java) | **Skeleton:** Compare nums[left] + nums[right] with target; move left if small, right if large. **Contract:** left and right bound the remaining sorted search space. If sum is too small, only moving left rightward can increase it; if sum is too large, only moving right leftward can decrease it. Return the required one-based indices when the sum equals target. | **Trap:** return one-based indices; move exactly one side according to the sum **Mutation:** Remove sorted order but keep original index output. -> HashMap: Complement lookup replaces endpoint elimination. |
| **Two Pointers -> Two pointers / stack** | **Family skeleton:** place boundaries -> evaluate current pair/span -> move only the boundary whose movement safely discards candidates -> preserve best/validity | |
| [Trapping Rain Water](../../src/main/java/org/chijai/day3/session2/prefix/suffix/TrappingRainwater.java) | **Skeleton:** Move the side with lower height, update max, add max-height when bounded. **Contract:** leftMax and rightMax are the best walls already seen from each side. The side with smaller max determines trapped water there because the opposite side is already high enough. Move that side inward and add max - height only after updating the side max. | **Trap:** update side max before adding trapped water **Mutation:** Change the elevation map from one dimension to a 2D grid. -> Min-Heap BFS: Water escapes through the globally lowest processed boundary. |
| **Two Pointers -> Partition / Dutch flag** | **Family skeleton:** place boundaries -> evaluate current pair/span -> move only the boundary whose movement safely discards candidates -> preserve best/validity | |
| [Sort Colors](../../src/main/java/org/chijai/day1/Arrays/session1/SortColors.java) | **Skeleton:** Use low, mid, high; swap 0 to low, 2 to high, advance mid on 1. **Contract:** Maintain three regions: [0, low) are 0s, [low, mid) are 1s, and (high, end] are 2s. A 0 swaps to low and advances both low and mid; a 2 swaps to high and only high moves because the incoming value is unclassified. A 1 advances mid. | **Trap:** after swapping 2, do not advance mid **Mutation:** Increase from three colors to k arbitrary values. -> Counting Sort: Three fixed regions no longer represent every color. |
| **Two Pointers -> Linked list two pointers** | **Family skeleton:** place boundaries -> evaluate current pair/span -> move only the boundary whose movement safely discards candidates -> preserve best/validity | |
| [Remove Nth Node From End of List](../../src/main/java/org/chijai/day4/LinkedList/session1/Intersection.java) | **Skeleton:** Initialize pointers, compare current state, move the pointer whose movement is justified. **Contract:** Shrink the search space by moving the pointer that can still improve the answer. | **Trap:** move only the provably discardable side **Mutation:** Provide the node to delete in a doubly linked list. -> Direct Pointer Deletion: Prev/next links remove it in O(1) without a lead-gap scan. |
| **Two Pointers -> Expand around center** | **Family skeleton:** place boundaries -> evaluate current pair/span -> move only the boundary whose movement safely discards candidates -> preserve best/validity | |
| [Longest Palindromic Substring](../../src/main/java/org/chijai/day3/session3/LongestPalindromicSubstring.java) | **Skeleton:** For each index, expand(i,i) and expand(i,i+1), update best start/length. **Contract:** Expand around every odd and even center and keep the longest span. | **Trap:** expand both odd and even centers; convert final bounds without an off-by-one **Mutation:** Allow skipping characters while preserving order. -> Interval DP: Contiguity and center expansion no longer characterize the answer. |
| **Heap / Priority Queue -> Heap / divide and conquer** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [Merge K Sorted Lists](../../src/main/java/org/chijai/day4/LinkedList/session4/MergeKSortedLists.java) | **Skeleton:** Push non-null heads, poll min, append it, push its next. **Contract:** The heap stores one current head per non-empty list, so the heap root is the globally smallest available node. Poll that node, append it to tail, then push its next node because that next node just became the list's candidate. Do not scan all k heads on every append. | **Trap:** push polled.next, not every node upfront **Mutation:** Reduce k lists to exactly two sorted lists. -> Linked List Pointers: A dummy tail and two pointers pick the smaller head directly. |
| **Heap / Priority Queue -> Frequency + heap/bucket** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [Top K Frequent Elements](../../src/main/java/org/chijai/day7/session1/heap/TopKFrequentElements.java) | **Skeleton:** Build frequency map, then use bucket lists by frequency or a min-heap of size k. **Contract:** First count exact frequencies, then select by frequency rather than by value. A size-k min-heap keeps the k strongest candidates by evicting the current weakest. If using buckets, bucket index is frequency. | **Trap:** order by frequency, not value; keep exactly k candidates **Mutation:** Ask for every distinct value sorted by frequency. -> Full Sort: Bounded top-k retention no longer saves ordering work. |
| [Sort Characters By Frequency](../../src/main/java/org/chijai/day7/session1/heap/TopKFrequentElements.java) | **Skeleton:** Count chars, bucket by frequency or heap entries, append char repeated count times. **Contract:** Frequency map plus bucket/heap outputs characters from highest count to lowest. | **Trap:** emit each character frequency times; comparator ties must remain consistent **Mutation:** Receive characters online and repeatedly ask for the current most frequent. -> Heap + Frequency Map: The ranking must be maintained across updates. |
| **Heap / Priority Queue -> Intervals / heap** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [Meeting Rooms II](../../src/main/java/org/chijai/day1/Arrays/session4/Intervals/IntervalActiveMinHeap.java) | **Skeleton:** Sort intervals, pop heap while end <= start, push current end, track max heap size. **Contract:** Sort meetings by start time and keep active meeting end times in a min-heap. If the earliest end is <= current start, that room is reusable before adding the current meeting. Heap size after adding is active rooms; maximum size is rooms needed. | **Trap:** reuse when earliestEnd <= start **Mutation:** Ask whether a person can attend all meetings. -> Intervals / Sorting Greedy: After sorting by start, only the previous end must be checked. |
| **Heap / Priority Queue -> Two heaps** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [Find Median From Data Stream](../../src/main/java/org/chijai/day7/session1/heap/Median.java) | **Skeleton:** Push into maxHeap/minHeap, rebalance sizes, median is top or average of tops. **Contract:** The max-heap owns the lower half and the min-heap owns the upper half. Keep sizes balanced so they differ by at most one and every lower value is <= every upper value. Median is one heap top or the average of both tops. | **Trap:** rebalance heaps after each insert **Mutation:** Ask for the median of every fixed-size sliding window. -> Two Heaps + Lazy Deletion: Values must expire as the window advances. |
| **Heap / Priority Queue -> Greedy / heap** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [Task Scheduler](../../src/main/java/org/chijai/day7/session1/heap/TaskScheduler.java) | **Skeleton:** Use maxFreq and countMax: max(tasks.length, (maxFreq-1)*(n+1)+countMax). **Contract:** CPU idles only when the most frequent tasks cannot be spaced by cooldown gaps. | **Trap:** cooldown applies between equal tasks; include unavoidable idle slots **Mutation:** Give each task type its own cooldown. -> Heap Simulation: The closed-form maximum-frequency formula no longer captures release times. |
| **Heap / Priority Queue -> Min-heap size K** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [Kth Largest Element In An Array](../../src/main/java/org/chijai/day7/session1/heap/KthLargestInStream.java) | **Skeleton:** Push each number, pop when heap size > k, return heap top. **Contract:** A size-k min-heap keeps the k largest seen so far; top is kth largest. | **Trap:** a size-k min-heap evicts only when size > k; answer is the root **Mutation:** Require expected linear time on one static array. -> Quickselect: Partitioning avoids maintaining a logarithmic heap. |
| [Kth Largest Element In A Stream](../../src/main/java/org/chijai/day7/session1/heap/KthLargestInStream.java) | **Skeleton:** On add, push value, trim heap to k, return heap.peek(). **Contract:** Maintain a size-k min-heap after every add; top is the kth largest in the stream. | **Trap:** initialize through the same add invariant; root is valid only after k elements **Mutation:** Ask for the kth smallest as well as kth largest after every update. -> Two Ordered Multisets: One bounded min-heap cannot expose both tails. |
| **Heap / Priority Queue -> Heap / quickselect** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [K Closest Points To Origin](../../src/main/java/org/chijai/day7/session1/heap/KClosestPointsToOrigin.java) | **Skeleton:** Use max-heap of size k by distance, or quickselect by squared distance. **Contract:** Keep the k smallest squared distances; compare without taking square roots. | **Trap:** compare squared distance without overflow; cap the retained heap at k **Mutation:** Ask for all points ordered by distance. -> Full Sort: Every relative rank is now required. |
| **Heap / Priority Queue -> Bounded heap with tie ordering** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [Top K Frequent Words](../../src/main/java/org/chijai/day7/session1/heap/TopKFrequentElements.java) | **Skeleton:** Count words, offer each distinct word, evict when size > k, then remove from weakest to strongest and prepend to produce final order. **Contract:** A size-k min-heap stores the k strongest words, with the weakest winner at the root: lower frequency, or lexicographically larger on a tie. | **Trap:** tie makes lexicographically larger word the weaker heap root **Mutation:** Ask top suggestions for every typed prefix. -> Trie + Bounded Ranking: Frequency selection must be scoped to shared prefixes. |
| **Heap / Priority Queue -> Bounded frequency buckets** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [H-Index](../../src/main/java/org/chijai/day7/session1/heap/TopKFrequentElements.java) | **Skeleton:** Accumulate paper counts from h = n downward; the first h with papers >= h is the maximum valid H-index. **Contract:** buckets[h] counts papers with exactly h citations, except every citation >= n is capped into bucket n. | **Trap:** cap citations at n; first papers >= h while scanning down **Mutation:** Guarantee citations are sorted and ask for h only once. -> Binary Search: The h-feasibility boundary is monotonic by index. |
| **Heap / Priority Queue -> Heap / ranking** | **Family skeleton:** define heap ownership + comparator -> offer eligible candidate -> evict/rebalance only when invariant requires -> read answer from valid root(s) | |
| [Award Top K Hotels](../../src/main/java/org/chijai/day7/session1/heap/AwardTopKHotels.java) | **Skeleton:** Build keyword set, count matches per hotel review, then sort or heap by score/id. **Contract:** Score each hotel by keyword hits, then rank by score and tie-breaker. | **Trap:** apply score ties deterministically and preserve the required hotel ordering **Mutation:** Ask for the complete hotel ranking rather than top k. -> Full Sort: Every score tie and rank must be materialized. |
| **Tree BFS / Level Order -> Tree traversal** | **Family skeleton:** queue root -> snapshot level size -> process exactly that level -> enqueue children for the next level -> finalize level output | |
| [Binary Tree Level Order Traversal](../../src/main/java/org/chijai/day6/trees/session1/BinaryTreeTraversal.java) | **Skeleton:** For each level, poll size nodes, collect values, enqueue children. **Contract:** At the start of each outer loop, queue.size() is exactly the number of nodes in the current level. Process exactly that many nodes, appending their children for the next level. Never let newly enqueued children leak into the current level. | **Trap:** capture queue size before pushing children **Mutation:** Ask only for maximum tree depth. -> Tree DFS: A subtree height return replaces level snapshots. |
| **Tree BFS / Level Order -> Tree BFS / DFS** | **Family skeleton:** queue root -> snapshot level size -> process exactly that level -> enqueue children for the next level -> finalize level output | |
| [Binary Tree Right Side View](../../src/main/java/org/chijai/day6/trees/session1/BinaryTreeSideView.java) | **Skeleton:** For each level size, process nodes and record value when i == size - 1. **Contract:** The last node seen at each BFS level is visible from the right. | **Trap:** snapshot each level size and record only that level's final visible node **Mutation:** Ask for the leftmost and rightmost value at every depth. -> Tree BFS: Each level now needs both boundary positions. |
| **Tree DFS / Recursion -> Tree DFS / stack** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Validate Binary Search Tree](../../src/main/java/org/chijai/day6/trees/session1/BinaryTreeInorderTraversal.java) | **Skeleton:** DFS with low/high bounds, reject value <= low or >= high, recurse tightened bounds. **Contract:** Each node must be strictly greater than its inherited lower bound and strictly less than its inherited upper bound. Left children tighten the upper bound; right children tighten the lower bound. Parent-child checks alone miss ancestor violations. | **Trap:** strict bounds; ancestor violations matter **Mutation:** Ask to repair exactly two swapped BST values. -> Inorder Violation Tracking: Boolean bounds do not identify the offenders. |
| [Binary Tree Inorder Traversal](../../src/main/java/org/chijai/day6/trees/session1/BinaryTreeInorderTraversal.java) | **Skeleton:** Push left chain, pop node, visit, then go right. **Contract:** Inorder is left, node, right; for BST it yields sorted order. | **Trap:** visit left, node, right; iterative traversal must push the full left chain **Mutation:** Require values grouped by depth rather than inorder position. -> Tree BFS: Queue levels replace the traversal stack. |
| [Binary Tree Postorder Traversal](../../src/main/java/org/chijai/day6/trees/session1/BinaryTreeInorderTraversal.java) | **Skeleton:** Use recursion or stack with last-visited tracking; visit after left and right. **Contract:** Postorder visits children before the node, useful when parent depends on subtree results. | **Trap:** visit both children before the node; do not emit on first stack sight **Mutation:** Ask for parent-before-children serialization. -> Preorder DFS: The required emission point moves before recursive calls. |
| [Binary Tree Preorder Traversal](../../src/main/java/org/chijai/day6/trees/session1/BinaryTreeInorderTraversal.java) | **Skeleton:** Visit node, then left, then right; iterative stack pushes right before left. **Contract:** Preorder visits node before children, useful for serialization and copying structure. | **Trap:** visit node before children; push right before left in an iterative stack **Mutation:** Ask to delete every subtree safely after processing children. -> Postorder DFS: Parent work must wait for both child results. |
| **Tree DFS / Recursion -> Tree DFS return contract** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Lowest Common Ancestor Of A Binary Tree](../../src/main/java/org/chijai/day6/trees/session1/LCA.java) | **Skeleton:** Return node if null/p/q; ask left/right; if both non-null return root else non-null side. **Contract:** The helper returns a found target or an LCA from that subtree. If both left and right return non-null, current is the split point. If only one side returns non-null, pass that result upward. | **Trap:** return subtree evidence upward; current is LCA only when both sides report a target **Mutation:** Add parent pointers to every node. -> Ancestor Alignment: Root-based subtree recursion is unnecessary when both nodes can walk upward. |
| [Lowest Common Ancestor Of A Binary Tree II](../../src/main/java/org/chijai/day6/trees/session1/LCA.java) | **Skeleton:** DFS returns found node/count flags; only accept LCA when both p and q are found. **Contract:** Same split-point idea, but verify both targets actually exist. | **Trap:** the split-point result is valid only after confirming both targets exist **Mutation:** Guarantee both targets exist. -> Standard LCA DFS: Existence counters can be removed. |
| [Lowest Common Ancestor Of A Binary Tree III](../../src/main/java/org/chijai/day6/trees/session1/LCA.java) | **Skeleton:** Move a and b upward; when null redirect to the other node; meeting is LCA. **Contract:** With parent pointers, walk ancestors or switch pointers like linked-list intersection. | **Trap:** compare node identity through parent chains; do not assume equal depths **Mutation:** Remove parent pointers and provide only the root. -> Tree DFS Return Contract: Ancestor paths must be discovered from the root. |
| [Lowest Common Ancestor Of A Binary Tree IV](../../src/main/java/org/chijai/day6/trees/session1/LCA.java) | **Skeleton:** Return root if in target set; combine child returns and current membership. **Contract:** For many target nodes, current node is answer when multiple target paths meet. | **Trap:** treat targets as a set and stop ownership at the first multi-target split **Mutation:** Reduce the target set to exactly two guaranteed nodes. -> Standard LCA DFS: Set membership/count aggregation collapses to two target checks. |
| **Tree DFS / Recursion -> BST inorder** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Kth Smallest Element in a BST](../../src/main/java/org/chijai/day6/trees/session3/KthSmallestElementInBST.java) | **Skeleton:** Iterative inorder with stack, decrement k on visit, return when k hits zero. **Contract:** BST inorder traversal visits values in ascending order. Decrement k exactly when visiting the node itself, after left subtree and before right subtree. The kth visit is the answer. | **Trap:** decrement k on the inorder node visit, not when pushing or returning **Mutation:** Ask many kth-smallest queries while the BST is updated. -> Order-Statistic Tree: Subtree sizes make rank queries and updates logarithmic. |
| [Recover Binary Search Tree](../../src/main/java/org/chijai/day6/trees/session2/RecoverBST.java) | **Skeleton:** Track prev, first, second during inorder; after traversal swap first.val and second.val. **Contract:** Inorder traversal should be sorted; the two broken nodes appear at one or two inversions. | **Trap:** capture both inorder inversions; non-adjacent swaps require first and last offenders **Mutation:** Allow any number of misplaced values and ask to restore sorted order. -> Inorder Collect + Sort: Two-inversion logic no longer identifies all corrections. |
| [Binary Search Tree Iterator](../../src/main/java/org/chijai/day6/trees/session2/RecoverBST.java) | **Skeleton:** pushLeft(root); next() pops, then pushLeft(node.right); hasNext() checks stack. **Contract:** Maintain a stack of the current left spine so next() returns the next inorder value lazily. | **Trap:** push the entire left spine initially and after every popped node's right child **Mutation:** Require predecessor as well as successor iteration. -> Bidirectional Stack/Parent Links: One left-spine stack supports only forward order. |
| [Convert BST To Greater Tree](../../src/main/java/org/chijai/day6/trees/session2/RecoverBST.java) | **Skeleton:** Traverse right, add node.val into running sum, rewrite node.val, then traverse left. **Contract:** Reverse inorder visits larger values first, so a running sum can rewrite each node. | **Trap:** reverse inorder is required; update the running sum before storing the node **Mutation:** Ask many range-sum queries without mutating the BST. -> Augmented BST / Prefix Index: Persistent subtree sums replace one destructive reverse-inorder pass. |
| **Tree DFS / Recursion -> Core tree patterns** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Diameter of Binary Tree](../../src/main/java/org/chijai/day6/trees/session3/BinaryTree.java) | **Skeleton:** Postorder compute heights, update max diameter with left+right, return max height+1. **Contract:** The helper returns height upward, but the global answer is the best leftHeight + rightHeight seen at any node. Update diameter before returning 1 + max(leftHeight, rightHeight). Do not return diameter as height. | **Trap:** return height, update diameter separately **Mutation:** Ask for the actual longest path nodes, not only its length. -> Tree DP + Parent Reconstruction: Height values need endpoint/choice ownership. |
| **Tree DFS / Recursion -> Tree path DFS / global answer** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Path Sum III](../../src/main/java/org/chijai/day6/trees/session4/BinaryTreePathProblems.java) | **Skeleton:** DFS with running sum, add count[sum-target], increment before children, decrement on backtrack. **Contract:** prefixSum counts belong only to the current root-to-node path. For current sum s, paths ending here with targetSum equal the number of earlier prefixes s - targetSum. Add current prefix before going down, then decrement it when backtracking. | **Trap:** decrement prefix count on backtrack **Mutation:** Change the tree to an array subarray-sum problem. -> Prefix/Suffix: A running prefix map over indices is enough. |
| [Sum Root To Leaf Numbers](../../src/main/java/org/chijai/day6/trees/session4/BinaryTreePathProblems.java) | **Skeleton:** DFS with value = value * 10 + node.val; return value at leaves, sum children otherwise. **Contract:** Carry the number formed so far; at a leaf, add it to the total. | **Trap:** append the digit before the leaf check; add only complete root-to-leaf numbers **Mutation:** Allow edges with arbitrary multi-digit weights and ask minimum path sum. -> Tree DFS Min DP: Decimal prefix construction becomes weighted optimization. |
| [Binary Tree Maximum Path Sum](../../src/main/java/org/chijai/day6/trees/session4/BinaryTreePathProblems.java) | **Skeleton:** Clamp child gains at zero, update global with node+left+right, return node+max(left,right). **Contract:** Helper returns best non-splitting gain; global answer may split through node. | **Trap:** clamp negative child gains to zero; return one branch but update global with two **Mutation:** Restrict the path to start at root and end at a leaf. -> Root-to-Leaf DFS: The two-branch global update is no longer legal. |
| [Path Sum](../../src/main/java/org/chijai/day6/trees/session4/BinaryTreePathProblems.java) | **Skeleton:** DFS with remaining sum; at leaf return remaining == node.val. **Contract:** Subtract node values along root-to-leaf paths and check target at leaf. | **Trap:** accept target equality only at a leaf, not at an internal prefix **Mutation:** Count every downward path, not only root-to-leaf paths. -> Prefix Sum on DFS Path: A boolean remaining-sum check misses arbitrary starts. |
| [Path Sum II](../../src/main/java/org/chijai/day6/trees/session4/BinaryTreePathProblems.java) | **Skeleton:** Add node, recurse children with remaining sum, copy on valid leaf, remove node. **Contract:** Backtrack the current root-to-leaf path and copy it when the target is hit. | **Trap:** copy the path on a matching leaf and remove the current node on every return **Mutation:** Ask only how many matching root-to-leaf paths exist. -> Tree DP Counting: Concrete path copying is no longer required. |
| **Tree DFS / Recursion -> BST property** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Lowest Common Ancestor Of A Binary Search Tree](../../src/main/java/org/chijai/day6/trees/session1/LCA_BST.java) | **Skeleton:** Loop from root; compare p and q to node.val and move left/right until they diverge. **Contract:** Use BST order to walk toward the split. If both targets are smaller than current, go left; if both are larger, go right. Otherwise current is where the two search paths diverge or one target equals current. | **Trap:** walk by both target values; the first split or equality is the answer **Mutation:** Remove the BST ordering guarantee. -> General Tree LCA DFS: Value comparison can no longer discard a subtree. |
| [Insert Into A Binary Search Tree](../../src/main/java/org/chijai/day6/trees/session1/LCA_BST.java) | **Skeleton:** Iterate or recurse: if val < node.val go left, else go right; attach new node at null. **Contract:** Use BST ordering to walk one branch until a null child is found, then insert there. | **Trap:** attach at the first null position and return the original root **Mutation:** Require the tree to remain height-balanced after insertion. -> AVL/Red-Black Tree: Rotations and balance metadata become part of the contract. |
| [Minimum Absolute Difference In BST](../../src/main/java/org/chijai/day6/trees/session1/LCA_BST.java) | **Skeleton:** Inorder traverse, track previous value and best difference. **Contract:** BST inorder is sorted, so minimum difference is between adjacent inorder values. | **Trap:** compare adjacent inorder values and initialize previous-node state safely **Mutation:** Remove BST ordering. -> Sort Values: Inorder is no longer sorted, so values must be ordered explicitly before adjacent comparison. |
| [Range Sum Of BST](../../src/main/java/org/chijai/day6/trees/session1/LCA_BST.java) | **Skeleton:** If node < low go right, if node > high go left, else add node and both sides. **Contract:** BST ordering lets you prune subtrees outside [low, high]. | **Trap:** include both bounds; prune only the subtree BST ordering proves irrelevant **Mutation:** Allow frequent updates and repeated range-sum queries. -> Augmented Balanced BST: Subtree aggregates amortize the new workload. |
| [Search In A Binary Search Tree](../../src/main/java/org/chijai/day6/trees/session1/LCA_BST.java) | **Skeleton:** While node != null and node.val != val, move left if val < node.val else right. **Contract:** Compare target with node value and move only to the branch that can still contain it. | **Trap:** choose one branch from the comparison; do not search both subtrees **Mutation:** Remove the BST order property. -> Tree DFS/BFS: Both children can contain the target. |
| **Tree DFS / Recursion -> Tree DFS/BFS** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Invert Binary Tree](../../src/main/java/org/chijai/day6/trees/session3/InvertBinaryTree.java) | **Skeleton:** DFS or BFS each node, swap children, continue. **Contract:** Swap left and right at every node. | **Trap:** swap both child references once per node; preserve the new children for recursion **Mutation:** Ask whether two trees are mirrors without modifying either. -> Paired Tree DFS: Corresponding opposite children must be compared. |
| **Tree DFS / Recursion -> Tree recursion / hashmap index** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Construct Binary Search Tree From Preorder Traversal](../../src/main/java/org/chijai/day6/trees/session2/ConstructTree.java) | **Skeleton:** Use index over preorder and recursive upper/lower bounds to build nodes. **Contract:** Preorder root plus BST bounds tells where each next value belongs. | **Trap:** advance the shared index only when the value fits the inherited upper bound **Mutation:** Provide arbitrary preorder plus inorder for a non-BST. -> Index-Map Tree Reconstruction: Value bounds no longer identify subtree limits. |
| [Verify Preorder Serialization Of A Binary Tree](../../src/main/java/org/chijai/day6/trees/session2/ConstructTree.java) | **Skeleton:** For each token decrement slots, fail below zero, add two slots if token is not #. **Contract:** Slots start at one; every node consumes a slot, non-null nodes create two. | **Trap:** consume one slot per token and add two only for non-null nodes; slots may never go negative **Mutation:** Ask to reconstruct the tree, not only validate slots. -> Preorder Deserialization: Nodes and recursive child ownership must be materialized. |
| [Construct Binary Tree From Inorder And Postorder Traversal](../../src/main/java/org/chijai/day6/trees/session2/ConstructTree.java) | **Skeleton:** Pop root from postorder end, build right then left using inorder bounds. **Contract:** Postorder last is root; inorder index splits left and right subtrees. | **Trap:** consume postorder from the end and build right before left **Mutation:** Replace postorder with preorder. -> Preorder + Inorder Reconstruction: Consume roots from the front and build left before right. |
| [Construct Binary Tree From Preorder And Inorder Traversal](../../src/main/java/org/chijai/day6/trees/session2/ConstructTree.java) | **Skeleton:** Read preorder index, split by inorder map, recursively build left and right ranges. **Contract:** Preorder first is root; inorder index splits left and right subtrees. | **Trap:** split by the inorder index and keep preorder/subtree ranges aligned **Mutation:** Replace preorder with postorder. -> Postorder + Inorder Reconstruction: Consume roots from the end and build right first. |
| **Tree DFS / Recursion -> Tree BFS/DFS serialization** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Serialize And Deserialize Binary Tree](../../src/main/java/org/chijai/day6/trees/session2/SerializeAndDeserializeBinaryTree.java) | **Skeleton:** Preorder/BFS serialize with # for null; deserialize by consuming tokens in same order. **Contract:** Include null markers so structure can be reconstructed unambiguously. | **Trap:** encode null markers and delimiters explicitly; deserialize in the same traversal order **Mutation:** Require sorted-key range queries after reconstruction. -> BST Encoding: Ordering can eliminate null markers and support search semantics. |
| **Tree DFS / Recursion -> Tree converted to graph + BFS distance** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [All Nodes Distance K in Binary Tree](../../src/main/java/org/chijai/day6/trees/session2/BurnBinaryTree.java) | **Skeleton:** Build node-to-parent links, BFS from target with visited marked on enqueue, advance exactly k levels, then return the frontier. **Contract:** Parent links turn the tree into an undirected graph; BFS level d contains exactly the nodes distance d from target. | **Trap:** add parent edges; stop after exactly k BFS levels **Mutation:** Ask the same distance-k query for many targets. -> Preprocessing / Centroid Decomposition: Rebuilding parent BFS per query repeats whole-tree work. |
| **Tree DFS / Recursion -> Tree converted to graph + BFS spread** | **Family skeleton:** define helper return -> solve children once -> combine at current node -> update separate global state if needed -> return parent contract | |
| [Amount of Time for Binary Tree to Be Infected](../../src/main/java/org/chijai/day6/trees/session2/BurnBinaryTree.java) | **Skeleton:** Build node-to-parent links, BFS from start with visited on enqueue, and count completed spreading levels until the frontier is empty. **Contract:** Parent links expose all three directions; each BFS level is one infection minute from the start node. | **Trap:** mark on enqueue; do not count a nonexistent final minute **Mutation:** Give weighted transmission times on edges. -> Dijkstra: BFS layers no longer represent equal elapsed time. |
| **Graph DFS / Components -> Matrix DFS/BFS components** | **Family skeleton:** start each unowned component/path -> mark ownership before descent -> explore valid neighbors -> restore only for path-local ownership | |
| [Number Of Islands](../../src/main/java/org/chijai/day8/graph/session1/Islands.java) | **Skeleton:** Scan grid; on '1', increment count and DFS/BFS four directions marking visited/water. **Contract:** A new island is counted only when an unvisited land cell is first found. DFS/BFS then owns and marks that entire 4-directional land component. Water and already visited land contribute nothing. | **Trap:** mark visited before exploring neighbors **Mutation:** Add land cells online and ask island count after each addition. -> Union Find: Incremental component merges replace repeated full-grid traversal. |
| [Pacific Atlantic Water Flow](../../src/main/java/org/chijai/day8/graph/session1/Islands.java) | **Skeleton:** Mark cells reachable from Pacific border and Atlantic border; answer intersection. **Contract:** Reverse the problem: start from each ocean border and move to neighbors with height >= current height. A cell can reach an ocean if the reversed search can reach the cell from that ocean. The answer is cells marked by both ocean searches. | **Trap:** reverse flow: move to >= height **Mutation:** Ask for the minimum downhill steps from each cell to an ocean. -> Multi-Source BFS/Dijkstra: Reachability sets no longer capture distance. |
| [Surrounded Regions](../../src/main/java/org/chijai/day8/graph/session1/Islands.java) | **Skeleton:** DFS/BFS border O cells as safe, flip remaining O to X, restore safe marks. **Contract:** Only O cells connected to the border are safe. Mark all border-connected O cells first, then flip every remaining O to X and restore safe marks to O. Do not flip before proving border reachability. | **Trap:** mark border-safe O before flipping **Mutation:** Ask for the size of every enclosed region without modifying the board. -> Component DFS: Each component must be measured and classified before output. |
| [Number Of Closed Islands](../../src/main/java/org/chijai/day8/graph/session1/Islands.java) | **Skeleton:** DFS each land component, return false if any cell touches border, mark visited. **Contract:** A closed island is a land component that never touches the grid boundary. | **Trap:** border-connected land is not closed; either erase borders first or propagate closure with AND **Mutation:** Add land online and query whether components remain closed. -> Union Find with Border Flag: Static DFS ownership must survive incremental merges. |
| [Max Area Of Island](../../src/main/java/org/chijai/day8/graph/session1/Islands.java) | **Skeleton:** On each unvisited land cell, DFS four directions accumulating area. **Contract:** DFS each land component and return its cell count; keep the maximum. | **Trap:** reset area per component and mark each land cell before expanding **Mutation:** Add land cells online and report maximum area after each addition. -> Union Find with Component Size: Incremental merges update area without rescanning. |
| **Graph DFS / Components -> Matrix DFS/BFS** | **Family skeleton:** start each unowned component/path -> mark ownership before descent -> explore valid neighbors -> restore only for path-local ownership | |
| [Flood Fill](../../src/main/java/org/chijai/day8/graph/session1/FloodFill.java) | **Skeleton:** If oldColor == newColor return; DFS/BFS neighbors with oldColor and recolor them. **Contract:** Only cells connected to the start and equal to the original color can change. If newColor equals original color, return early to avoid revisiting forever. Mark/recolor before recursing to prevent cycles. | **Trap:** return early when newColor == original **Mutation:** Assign different costs to crossing adjacent cells and ask cheapest recoloring reach. -> Dijkstra: Connectivity alone no longer chooses the minimum-cost frontier. |
| **Graph DFS / Components -> BFS/DFS coloring** | **Family skeleton:** start each unowned component/path -> mark ownership before descent -> explore valid neighbors -> restore only for path-local ownership | |
| [Is Graph Bipartite?](../../src/main/java/org/chijai/day8/graph/session2/GraphBipartite.java) | **Skeleton:** For each uncolored node, BFS/DFS assign colors and fail on same-color neighbor. **Contract:** color[node] records which side of the partition owns the node. Each edge must connect opposite colors; an uncolored neighbor receives the opposite color. A same-color edge is an immediate contradiction. | **Trap:** visited is not enough; check colors **Mutation:** Ask for a valid dependency order in a directed graph. -> Topological Sort: Two-color ownership does not model prerequisite direction. |
| **Graph DFS / Components -> Graph DFS/BFS clone** | **Family skeleton:** start each unowned component/path -> mark ownership before descent -> explore valid neighbors -> restore only for path-local ownership | |
| [Clone Graph](../../src/main/java/org/chijai/day8/graph/session2/CloneGraph.java) | **Skeleton:** DFS/BFS: create clone if absent, then connect cloned neighbors from the map. **Contract:** Map original node to cloned node before cloning neighbors to handle cycles. | **Trap:** store original-to-clone before recursing so cycles reuse the same clone **Mutation:** Ask only whether all nodes are reachable from the start. -> Graph DFS: Clone identity allocation is unnecessary. |
| **Graph DFS / Components -> Connectivity + cycle** | **Family skeleton:** start each unowned component/path -> mark ownership before descent -> explore valid neighbors -> restore only for path-local ownership | |
| [Graph Valid Tree](../../src/main/java/org/chijai/day8/graph/session2/GraphBipartite.java) | **Skeleton:** Reject edge count != n - 1, traverse from one node while skipping the parent edge, and require every node to be visited. **Contract:** A valid undirected tree has exactly n - 1 edges and all n nodes in one connected component. | **Trap:** require n - 1 edges and one connected component **Mutation:** Add edges online and reject the first cycle. -> Union Find: Each new edge only needs component-root comparison. |
| **Graph DFS / Components -> Bipartite coloring** | **Family skeleton:** start each unowned component/path -> mark ownership before descent -> explore valid neighbors -> restore only for path-local ownership | |
| [Possible Bipartition](../../src/main/java/org/chijai/day8/graph/session2/GraphBipartite.java) | **Skeleton:** Build an undirected dislike graph; for every uncolored component, assign opposite colors by BFS/DFS and fail on a same-color edge. **Contract:** color[x] is the group assigned to person x; every dislike edge must connect opposite colors. | **Trap:** scan disconnected components; fail only on same-color edge **Mutation:** Add dislike edges online and query consistency after each edge. -> DSU with Parity: Static recoloring cannot cheaply preserve dynamic opposite-set constraints. |
| **Graph DFS / Components -> Matrix DFS** | **Family skeleton:** start each unowned component/path -> mark ownership before descent -> explore valid neighbors -> restore only for path-local ownership | |
| [Coloring A Border](../../src/main/java/org/chijai/day8/graph/session1/ColoringABorder.java) | **Skeleton:** DFS component, mark a cell as border if it touches outside grid or different color. **Contract:** Only cells on the component boundary get recolored; interior cells keep original color. | **Trap:** recolor only component cells touching outside/non-component; preserve interior cells **Mutation:** Ask for distance of every component cell from the border. -> Multi-Source BFS: Layer number, not binary border membership, becomes the output. |
| **Topological Sort -> Topological sort / cycle** | **Family skeleton:** build directed edges + dependency counts -> enqueue currently unlocked nodes -> consume edges on pop -> detect leftovers/ambiguity from the required output | |
| [Course Schedule](../../src/main/java/org/chijai/day8/graph/session2/CourseSchedule.java) | **Skeleton:** Build prerequisite->course graph, queue indegree-zero courses, decrement neighbors, compare processed count. **Contract:** indegree is the number of prerequisites still unmet for a course. Only zero-indegree courses can enter the queue, and processing one course decrements its dependents. If processed count is less than n, a cycle kept some courses locked. | **Trap:** processed count detects cycle **Mutation:** Ask for one valid course order. -> Topological Ordering: Cycle feasibility now must also record dequeue order. |
| [Course Schedule II](../../src/main/java/org/chijai/day8/graph/session2/CourseSchedule.java) | **Skeleton:** Build prerequisite->course graph, queue indegree-zero courses, append order, fail if processed < n. **Contract:** A course is appended to the answer only when its indegree has dropped to zero. Processing that course consumes it as a prerequisite and unlocks dependents by decrementing indegree. If the final order length is not n, return empty because a cycle remains. | **Trap:** return empty if order length < n **Mutation:** Ask whether the valid order is unique. -> Unique Kahn BFS: Queue size must stay one at every step. |
| **Topological Sort -> Topological trimming** | **Family skeleton:** build directed edges + dependency counts -> enqueue currently unlocked nodes -> consume edges on pop -> detect leftovers/ambiguity from the required output | |
| [Minimum Height Trees](../../src/main/java/org/chijai/day8/graph/session3/MinHTree.java) | **Skeleton:** Build graph/degrees, queue degree-1 leaves, remove layers while remainingNodes > 2. **Contract:** Peel all current leaves together until one or two centroid roots remain. | **Trap:** the input must be a tree; trim simultaneous leaf layers until at most two centers remain **Mutation:** Add arbitrary cycles to the graph and ask for minimum eccentricity roots. -> All-Pairs/Repeated BFS: Leaf trimming relies on the input being a tree. |
| **Topological Sort -> Kahn BFS by levels** | **Family skeleton:** build directed edges + dependency counts -> enqueue currently unlocked nodes -> consume edges on pop -> detect leftovers/ambiguity from the required output | |
| [Parallel Courses](../../src/main/java/org/chijai/day8/graph/session2/CourseSchedule.java) | **Skeleton:** Queue all indegree-zero courses, process exactly queue.size() per semester, unlock dependents, and return -1 unless all courses were processed. **Contract:** The queue at a semester boundary contains every course currently unlocked; one complete Kahn level is one semester. | **Trap:** snapshot level size; return -1 if processed < n **Mutation:** Give each course a duration. -> DAG Longest-Path DP: One Kahn layer no longer equals one semester of elapsed time. |
| **Topological Sort -> Constraint inference + topological sort** | **Family skeleton:** build directed edges + dependency counts -> enqueue currently unlocked nodes -> consume edges on pop -> detect leftovers/ambiguity from the required output | |
| [Alien Dictionary](../../src/main/java/org/chijai/day8/graph/session2/CourseSchedule.java) | **Skeleton:** Reject a longer word before its exact prefix, deduplicate first-difference edges, then Kahn-sort all characters; return empty on a cycle. **Contract:** Only the first differing characters in adjacent sorted words create an ordering edge; every distinct character is still a graph node. | **Trap:** reject invalid prefix; only first difference creates edge **Mutation:** Ask whether the inferred alphabet order is unique. -> Unique Topological Sort: Every unlocked frontier must contain one letter. |
| **Topological Sort -> Reverse graph + outdegree elimination** | **Family skeleton:** build directed edges + dependency counts -> enqueue currently unlocked nodes -> consume edges on pop -> detect leftovers/ambiguity from the required output | |
| [Find Eventual Safe States](../../src/main/java/org/chijai/day8/graph/session2/CourseSchedule.java) | **Skeleton:** Reverse every edge, queue terminal nodes, decrement predecessor outdegrees, enqueue a predecessor at zero, then sort the safe nodes. **Contract:** outdegree[x] counts outgoing choices not yet proved safe; terminal nodes start safe with outdegree 0. | **Trap:** reverse edges; decrement predecessor outdegree **Mutation:** Ask for the minimum steps from each node to any terminal node. -> Reverse BFS Distance: Outdegree elimination needs a layer/distance state. |
| **Topological Sort -> Unique topological order** | **Family skeleton:** build directed edges + dependency counts -> enqueue currently unlocked nodes -> consume edges on pop -> detect leftovers/ambiguity from the required output | |
| [Sequence Reconstruction](../../src/main/java/org/chijai/day8/graph/session2/CourseSchedule.java) | **Skeleton:** Build deduplicated edges, require every target value to appear, reject queue.size() != 1 or a mismatched pop, and consume all target values. **Contract:** The target is uniquely reconstructible only when Kahn's frontier has exactly one node and that node equals nums[index] at every step. | **Trap:** queue size must equal 1; every nums value must appear **Mutation:** Ask for any valid reconstruction rather than proving uniqueness. -> Standard Topological Sort: Multiple zero-indegree choices become acceptable. |
| **Topological Sort -> Two-level topological sort** | **Family skeleton:** build directed edges + dependency counts -> enqueue currently unlocked nodes -> consume edges on pop -> detect leftovers/ambiguity from the required output | |
| [Sort Items by Groups Respecting Dependencies](../../src/main/java/org/chijai/day8/graph/session2/CourseSchedule.java) | **Skeleton:** VERIFY FROM SOURCE - confirm ungrouped-item normalization, item graph, group graph, and contiguous emission order before memorizing transitions. **Contract:** VERIFY FROM SOURCE - the local chapter records that item and group dependencies require two coordinated topological orders, but it does not provide a complete accepted implementation. | **Trap:** VERIFY FROM SOURCE - complete accepted implementation absent **Mutation:** Remove group-contiguity requirements. -> Single Topological Sort: The group-level dependency graph is no longer needed. |
| **Topological Sort -> Dependency transitive closure** | **Family skeleton:** build edges + indegree -> enqueue zero-indegree nodes -> pop and consume outgoing edges -> validate count/order requirement | |
| [Course Schedule IV](../../src/main/java/org/chijai/day8/graph/session2/CourseSchedule.java) | **Skeleton:** Seed direct prerequisite edges, compute transitive closure through every intermediate course, then answer each query from reachable[from][to]. **Contract:** reachable[a][b] means course a is a direct or indirect prerequisite of course b. | **Trap:** prerequisite edge direction; via loop must be outermost **Mutation:** Ask prerequisite reachability online as edges are added. -> Dynamic Reachability Index: One static transitive-closure table can become stale. |
| **Graph BFS / Shortest Path -> BFS shortest path** | **Family skeleton:** enqueue source(s) + mark visited -> pop in nondecreasing distance -> claim valid neighbors on enqueue -> stop at required frontier | |
| [Word Ladder](../../src/main/java/org/chijai/day8/graph/session3/WordLadder.java) | **Skeleton:** Queue begin word, generate one-letter mutations, visit dictionary words once per level. **Contract:** Each queued word has a distance from beginWord, and all one-letter transformations cost one step. Mark a word visited when enqueueing so it cannot be reached again at the same or greater distance. The first time endWord is generated or dequeued is the shortest length. | **Trap:** mark visited when enqueueing **Mutation:** Ask for every shortest transformation sequence. -> BFS + Backtracking: Distance layers must retain all shortest parents for reconstruction. |
| **Graph BFS / Shortest Path -> Multi-source BFS** | **Family skeleton:** enqueue every distance-0 source + mark -> process one frontier layer -> claim unvisited neighbors on enqueue -> advance distance/time once per layer | |
| [Rotting Oranges](../../src/main/java/org/chijai/day8/graph/session1/RottenOranges.java) | **Skeleton:** Queue all rotten cells, count fresh, process BFS levels, decrement fresh on infection. **Contract:** All initially rotten oranges are minute 0 BFS sources. One BFS layer is one minute, so minutes increments after processing the whole layer, not per orange. Mark a fresh orange rotten when enqueueing it and return -1 if fresh remains unreachable. | **Trap:** minutes per layer, not per cell **Mutation:** Give each cell a different rotting delay. -> Multi-Source Dijkstra: One BFS layer no longer equals one minute. |
| [01 Matrix](../../src/main/java/org/chijai/day8/graph/session1/Matrix01.java) | **Skeleton:** Queue every zero with distance 0, then relax unvisited neighbors to dist+1. **Contract:** All zero cells start in the queue with distance 0. A one cell gets its nearest-zero distance the first time BFS reaches it. Mark distance when enqueueing to avoid duplicate visits. | **Trap:** multi-source from zeros, not BFS from each one **Mutation:** Assign weighted movement costs between cells. -> Multi-Source Dijkstra: First unweighted discovery no longer proves minimum cost. |
| **Graph BFS / Shortest Path -> Dijkstra / graph** | **Family skeleton:** min-heap (distance,state) -> ignore stale entry -> relax outgoing edges -> push only improved distances | |
| [Network Delay Time](../../src/main/java/org/chijai/day8/graph/session2/NetworkDelayTime.java) | **Skeleton:** Build adjacency, push source distance 0, relax neighbors when a smaller distance is found. **Contract:** dist[node] is the best known time from the source. The min-heap always expands the currently smallest candidate distance; ignore stale heap entries larger than dist[node]. Weighted edges require Dijkstra, not plain BFS. | **Trap:** ignore stale heap distances **Mutation:** Make every edge weight exactly one. -> BFS: Priority ordering is unnecessary when all transitions have equal cost. |
| **Graph BFS / Shortest Path -> Matrix DFS/BFS components** | **Family skeleton:** enqueue source(s) + mark visited -> pop in nondecreasing distance -> claim valid neighbors on enqueue -> stop at required frontier | |
| [Number Of Provinces](../../src/main/java/org/chijai/day8/graph/session1/Islands.java) | **Skeleton:** Scan cities; when unvisited, count province and traverse connected cities from adjacency matrix. **Contract:** Each DFS/BFS from an unvisited city marks one connected province. | **Trap:** start one traversal per unvisited city; mark the start before scanning its row **Mutation:** Add connections online and query province count after each update. -> Union Find: Component roots update incrementally. |
| **Graph BFS / Shortest Path -> BFS + sorting** | **Family skeleton:** enqueue source(s) + mark visited -> pop in nondecreasing distance -> claim valid neighbors on enqueue -> stop at required frontier | |
| [K Highest Ranked Items Within A Price Range](../../src/main/java/org/chijai/day8/graph/session3/KHighestRankedItemsWithinAPriceRange.java) | **Skeleton:** BFS from start through passable cells; collect price-in-range items with distance and sort ranking. **Contract:** BFS by distance, collecting valid items and sorting tie-breaks by price,row,col. | **Trap:** finish a BFS distance layer before ranking; apply all tie keys in order **Mutation:** Remove distance from the ranking and ask global top k eligible cells. -> Bounded Heap: BFS layers are no longer part of the order. |
| **Dynamic Programming -> 1D DP** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [House Robber](../../src/main/java/org/chijai/day9/dp/session1/HouseRobber.java) | **Skeleton:** For each money, next = max(prev1, prev2 + money); shift prev2=prev1, prev1=next. **Contract:** At each house, the only relevant history is best if I skip this house versus rob it after the best before previous. The transition is max(previous best, bestBeforePrevious + current). Adjacent houses cannot both be chosen. | **Trap:** compute current from old prev1/prev2 before shifting them; never combine adjacent houses **Mutation:** Arrange houses in a circle. -> Two-Case DP: First and last cannot both be included, so solve two linear ranges. |
| [Climbing Stairs Fib](../../src/main/java/org/chijai/day9/dp/session1/ClimbingStairsFib.java) | **Skeleton:** Iterate two rolling values for ways to previous one and two steps. **Contract:** Ways to step n equals ways to n-1 plus ways to n-2. | **Trap:** define n = 0/1 bases explicitly and do not overwrite a value before using it **Mutation:** Allow an arbitrary set of jump lengths. -> Unbounded DP: Two rolling Fibonacci states no longer cover every predecessor. |
| **Dynamic Programming -> Unbounded knapsack DP** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Coin Change](../../src/main/java/org/chijai/day9/dp/session2/CoinChange.java) | **Skeleton:** Initialize dp[0]=0 and others INF; for amount 1..target, try every coin. **Contract:** dp[a] is the fewest coins needed to form amount a, with dp[0] = 0. For each amount, try every coin that can precede it and relax dp[a] from dp[a - coin] + 1. Unreachable states must stay as INF, not accidentally overflow into valid answers. | **Trap:** keep INF unreachable states safe **Mutation:** Ask to list every coin combination instead of the minimum count. -> Backtracking: Concrete choices become output and DP value compression is insufficient. |
| [Word Break](../../src/main/java/org/chijai/day9/dp/session2/CoinChange.java) | **Skeleton:** For each end i, set dp[i] if some dp[j] and s[j..i) is in the dictionary. **Contract:** dp[i] means prefix s[0..i) can be segmented into dictionary words. | **Trap:** seed dp[0] = true; substring end is exclusive and must follow a reachable prefix **Mutation:** Ask whether any dictionary word starts at each index. -> Trie: Trie traversal is the core operation. |
| [Climbing Stairs](../../src/main/java/org/chijai/day9/dp/session2/CoinChange.java) | **Skeleton:** Start ways(0)=1, ways(1)=1, then iterate next = oneBack + twoBack. **Contract:** Ways to reach n comes from n-1 plus n-2, with two rolling counts. | **Trap:** seed the empty/first step consistently; transition only from legal prior steps **Mutation:** Add blocked steps. -> Indexed DP: The fixed Fibonacci recurrence must check per-position legality. |
| [Min Cost Climbing Stairs](../../src/main/java/org/chijai/day9/dp/session2/CoinChange.java) | **Skeleton:** Iterate two rolling minimum costs and return min(cost to last, cost to second last). **Contract:** Cost to stand on step i is cost[i] plus min(previous one, previous two). | **Trap:** cost is paid when landing on a step; answer is min of the final two states **Mutation:** Allow jumps up to k steps. -> Sliding Minimum DP: Each state depends on a variable predecessor window. |
| [Perfect Squares](../../src/main/java/org/chijai/day9/dp/session2/CoinChange.java) | **Skeleton:** Initialize dp[0]=0; for x=1..n, dp[x]=1+min(dp[x-square]). **Contract:** dp[x] is the fewest square numbers summing to x; try each square as the last move. | **Trap:** seed dp[0] = 0 and try only squares <= current amount **Mutation:** Ask to output one minimum square decomposition. -> DP + Parent Reconstruction: Counts alone no longer satisfy the output. |
| **Dynamic Programming -> Grid DP** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Unique Paths](../../src/main/java/org/chijai/day9/dp/session1/UniquePaths.java) | **Skeleton:** Initialize first row/column to 1, fill dp[r][c] = dp[r-1][c] + dp[r][c-1]. **Contract:** Ways to a cell equal ways from top plus ways from left. | **Trap:** seed first row/column as one path; each cell combines top and left **Mutation:** Add obstacles to the grid. -> Grid DP with Legality: Blocked cells reset path count to zero. |
| **Dynamic Programming -> 0/1 knapsack DP** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Partition Equal Subset Sum](../../src/main/java/org/chijai/day9/dp/session2/PartitionEqualSubsetSum.java) | **Skeleton:** If total odd return false; update boolean dp from target down to num for each num. **Contract:** Equal partition means one subset must make total / 2, so odd total is impossible. dp[s] means processed numbers can form sum s, seeded by dp[0] = true. Iterate sums right to left so the current number cannot reuse a state it just created. | **Trap:** right-to-left DP; dp[0] seed **Mutation:** Ask to output every subset reaching half the total. -> Backtracking: The actual membership paths, not boolean reachability, are required. |
| **Dynamic Programming -> DP / patience sorting** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Longest Increasing Subsequence](../../src/main/java/org/chijai/day9/dp/session2/LIS.java) | **Skeleton:** For each x, lower_bound in tails and replace; answer is tails size. **Contract:** tails[len] is the smallest possible tail value for an increasing subsequence of length len + 1. Binary search the first tail >= x and replace it with x; this improves future options without changing known length incorrectly. Equal values replace, not extend, for strictly increasing subsequences. | **Trap:** first tail >= x; equal replaces **Mutation:** Ask how many longest increasing subsequences exist. -> Quadratic DP with Counts: Minimal tails preserve length but discard multiplicity. |
| [Number of Longest Increasing Subsequence](../../src/main/java/org/chijai/day9/dp/session2/LIS.java) | **Skeleton:** For each i, scan previous smaller j and update len[i] plus count[i]. **Contract:** Track both LIS length ending at i and how many ways achieve that length. | **Trap:** replace count on a longer predecessor, add count only on an equal-best predecessor **Mutation:** Ask only for the length of LIS. -> Binary Search Tails: Minimal tails are enough. |
| [Russian Doll Envelopes](../../src/main/java/org/chijai/day9/dp/session2/LIS.java) | **Skeleton:** Sort by width asc and height desc, then lower_bound heights to get LIS length. **Contract:** Sort width ascending, but equal width descending by height so equal-width envelopes cannot chain through LIS. Then run strict LIS on heights. Without descending tie-break, equal widths can be illegally nested. | **Trap:** equal width sorted by height desc **Mutation:** Remove width or guarantee all widths are unique and sorted. -> Plain LIS: Height LIS alone is valid. |
| **Dynamic Programming -> DP + binary search** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Maximum Profit In Job Scheduling](../../src/main/java/org/chijai/day2/session3/MaximumProfitInJobScheduling.java) | **Skeleton:** Sort by end, for each job compute max(skip, profit + dp[lastNonOverlapping]). **Contract:** Sort jobs by end time; dp[i] is best profit up to i, with binary search for compatible previous job. | **Trap:** binary search the first next start >= current end; keep jobs and DP in the same sort order **Mutation:** Remove job profits and maximize the number of non-overlapping jobs. -> Earliest-Finish Greedy: Every accepted job has equal value. |
| **Dynamic Programming -> Kadane / DP** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Kadane Max Sub Array](../../src/main/java/org/chijai/day9/dp/session1/KadaneMaxSubArray.java) | **Skeleton:** cur = max(x, cur + x); best = max(best, cur) for every element. **Contract:** Best subarray ending here is either current alone or previous best ending here plus current. | **Trap:** initialize from the first value so an all-negative array does not incorrectly return zero **Mutation:** Ask for maximum sum of non-adjacent values. -> House-Robber DP: Contiguous ending-state reasoning no longer applies. |
| **Dynamic Programming -> 2D DP** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Edit Distance](../../src/main/java/org/chijai/day9/dp/session2/EditDistance.java) | **Skeleton:** Initialize empty-string row/column; if chars equal copy diagonal else 1 + min(insert, delete, replace). **Contract:** dp[i][j] is the minimum edits to convert the first i chars of word1 to the first j chars of word2. Equal last chars inherit dp[i - 1][j - 1]; otherwise choose one plus insert, delete, or replace. Empty-prefix rows and columns are the base cases. | **Trap:** base rows/cols are prefix lengths **Mutation:** Disallow replacement and count only insertions/deletions. -> LCS-Derived DP: Shared subsequence length determines deletions and insertions. |
| [Distinct Subsequences](../../src/main/java/org/chijai/day9/dp/session2/EditDistance.java) | **Skeleton:** If chars match add skip and take counts; otherwise carry skip count. **Contract:** dp[i][j] counts ways the first i chars of s form the first j chars of t. Skipping s[i - 1] always contributes dp[i - 1][j]; if chars match, taking it also contributes dp[i - 1][j - 1]. dp[*][0] is 1 because empty t can always be formed by taking nothing. | **Trap:** dp[*][0] = 1 **Mutation:** Ask to print all matching subsequences for small input. -> Backtracking: Individual index paths become required output. |
| [Interleaving String](../../src/main/java/org/chijai/day9/dp/session2/EditDistance.java) | **Skeleton:** Fill dp by taking next char from s1 or s2 when it matches s3[i+j-1]. **Contract:** dp[i][j] means s3 prefix length i + j can be formed from s1 first i chars and s2 first j chars. The next char must come from s1[i - 1] or s2[j - 1] and match s3[i + j - 1]. Greedy fails when both strings offer the same character. | **Trap:** s3 index is i + j - 1 **Mutation:** Guarantee no position has both s1[i] and s2[j] matching s3. -> Greedy: The next source is forced. |
| [Longest Common Subsequence](../../src/main/java/org/chijai/day9/dp/session2/EditDistance.java) | **Skeleton:** If chars match use 1 + diagonal; otherwise max(top,left). **Contract:** dp[i][j] is the best subsequence length between two prefixes. | **Trap:** on mismatch take max(top,left); diagonal extension is only for equal characters **Mutation:** Ask only whether s is a subsequence of t. -> Two Pointers: A single forward scan is enough. |
| [Delete Operation for Two Strings](../../src/main/java/org/chijai/day9/dp/session2/EditDistance.java) | **Skeleton:** Compute LCS length, return word1.length + word2.length - 2 * lcs. **Contract:** Minimum deletions equals removing everything not in the LCS. | **Trap:** the state counts deletions for prefixes; initialize empty-prefix costs **Mutation:** Give different deletion costs per character. -> Weighted 2D DP: LCS length alone no longer determines minimum cost. |
| [Longest Palindromic Subsequence](../../src/main/java/org/chijai/day9/dp/session2/EditDistance.java) | **Skeleton:** Fill by increasing length: equal ends use 2 + dp[l+1][r-1], else max(drop left, drop right). **Contract:** dp[l][r] is best palindrome subsequence inside s[l..r]. | **Trap:** fill shorter intervals before longer ones; equal ends use inner interval + 2 **Mutation:** Require a contiguous palindrome. -> Expand Around Center: Skipping characters is no longer legal. |
| [Minimum ASCII Delete Sum for Two Strings](../../src/main/java/org/chijai/day9/dp/session2/EditDistance.java) | **Skeleton:** If chars match take diagonal; otherwise delete one side and add its ASCII cost. **Contract:** dp[i][j] is minimum ASCII deletion cost to make two prefixes equal. | **Trap:** empty-prefix bases are ASCII prefix sums, not character counts **Mutation:** Make every deletion cost one. -> LCS DP: Minimum deletions depend only on the longest common subsequence length. |
| **Dynamic Programming -> Stock DP variants** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Best Time to Buy and Sell Stock with Transaction Fee](../../src/main/java/org/chijai/day1/Arrays/session3/StockSeries2.java) | **Skeleton:** For each price: cash = max(cash, hold + price - fee); hold = max(hold, cash - price). **Contract:** Fee changes the sell transition; hold/cash states prevent double-counting fees. | **Trap:** charge the fee exactly once per transaction and update hold/cash from prior-day states **Mutation:** Set fee to zero. -> Greedy: Every positive rise is profitable. |
| [Best Time to Buy and Sell Stock with Cooldown](../../src/main/java/org/chijai/day1/Arrays/session3/StockSeries2.java) | **Skeleton:** For each price update sold = hold + price, hold = max(hold, rest - price), rest = max(rest, oldSold). **Contract:** Cooldown creates three states: hold, sold today, and rest. | **Trap:** buy from a rested state, not from cash created by yesterday's sell **Mutation:** Remove the cooldown rule. -> Greedy: The problem collapses to summing positive differences. |
| [Best Time to Buy and Sell Stock IV](../../src/main/java/org/chijai/day1/Arrays/session3/StockSeries2.java) | **Skeleton:** If k is large use stock II; otherwise update hold[t] and cash[t] for t = 1..k. **Contract:** For k transactions, each transaction layer has a hold and cash state. | **Trap:** transaction layers must not reuse same-day updates; handle k >= n/2 as unlimited **Mutation:** Make k unlimited or at least n/2. -> Greedy: All positive edges can be taken. |
| **Dynamic Programming -> Greedy / DP states** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Best Time to Buy and Sell Stock III](../../src/main/java/org/chijai/day1/Arrays/session3/StockSeries1.java) | **Skeleton:** Update buy1, sell1, buy2, sell2 for each price and return sell2. **Contract:** Four states track first buy, first sell, second buy, second sell. | **Trap:** preserve buy1/sell1/buy2/sell2 update semantics; at most two completed sells **Mutation:** Remove the two-transaction limit. -> Greedy: Each rising edge can be harvested independently. |
| **Dynamic Programming -> Contribution counting** | **Family skeleton:** define future-determining state -> seed smallest true states -> enumerate transitions -> fill only after dependencies -> return target state | |
| [Distinct Subsequences II](../../src/main/java/org/chijai/day10/session2/CountUniqueChars.java) | **Skeleton:** Maintain total distinct subsequences and lastContribution[char], updating total by new unique additions. **Contract:** Each char doubles subsequences, then subtracts subsequences counted before its previous occurrence. | **Trap:** subtract the previous contribution of the repeated character and normalize modulo **Mutation:** Ask to print every distinct subsequence for n <= 20. -> Backtracking: The concrete subsequences become the output. |
| **Backtracking / Combinatorial DFS -> Backtracking subsets** | **Family skeleton:** choose -> mutate path/ownership -> recurse -> restore exactly what this frame changed -> try next choice | |
| [Subsets](../../src/main/java/org/chijai/day11/backtracking/session1/Subsets.java) | **Skeleton:** Copy path on entry; for i from start, choose nums[i], recurse with i + 1, then remove the choice. **Contract:** path is one subset formed from indices before start; every recursion state itself is a valid answer. | **Trap:** copy path at every state; recurse with i + 1 **Mutation:** Ask only for the number of subsets reaching a target sum. -> Knapsack DP: Repeated sum states replace explicit subset emission. |
| **Backtracking / Combinatorial DFS -> Backtracking with candidate reuse** | **Family skeleton:** choose -> mutate path/ownership -> recurse -> restore exactly what this frame changed -> try next choice | |
| [Combination Sum](../../src/main/java/org/chijai/day11/backtracking/session1/CombinationSum.java) | **Skeleton:** When remaining == 0 copy path; choose candidate i <= remaining, recurse with i, then undo; prune larger sorted candidates. **Contract:** remaining is the target still unpaid and start prevents permutation duplicates; the same candidate may be reused. | **Trap:** recurse with i for reuse; undo path after return **Mutation:** Forbid candidate reuse. -> 0/1 Backtracking: The recursive next index changes from i to i + 1. |
| **Backtracking / Combinatorial DFS -> Grid path backtracking** | **Family skeleton:** choose -> mutate path/ownership -> recurse -> restore exactly what this frame changed -> try next choice | |
| [Word Search](../../src/main/java/org/chijai/day8/graph/session1/WordSearch.java) | **Skeleton:** Match board[r][c] to word[index], mark it for this path, recurse four directions with index + 1, then restore the cell. **Contract:** The path owns board cells temporarily while matching one word index. Mark the current cell before exploring neighbors, then restore it after recursion returns. A cell cannot be reused in the same path. | **Trap:** restore visited cell after recursion **Mutation:** Provide a dictionary of many words. -> Trie + Backtracking: Shared prefixes prune repeated board searches. |
| **Backtracking / Combinatorial DFS -> Position-choice backtracking** | **Family skeleton:** choose -> mutate path/ownership -> recurse -> restore exactly what this frame changed -> try next choice | |
| [Letter Combinations Of A Phone Number](../../src/main/java/org/chijai/day11/backtracking/session1/LetterCombinationsOfAPhoneNumber.java) | **Skeleton:** For each letter mapped from digits[index], append, recurse with index + 1, then delete; emit only when index == digits.length. **Contract:** index is the next digit to expand and path contains exactly one mapped letter for every earlier digit. | **Trap:** empty input returns empty; delete appended char **Mutation:** Ask only for the number of possible strings. -> Product Counting: Multiply mapping sizes without generating paths. |
| **Backtracking / Combinatorial DFS -> Permutation used-index state** | **Family skeleton:** choose -> mutate path/ownership -> recurse -> restore exactly what this frame changed -> try next choice | |
| [Permutations](../../src/main/java/org/chijai/day11/backtracking/session1/Permutations.java) | **Skeleton:** Loop all indices, choose only !used[i], mark and append, recurse, then remove and unmark; copy at size n. **Contract:** used[i] means index i is already owned by the current ordering; path length is the next permutation position. | **Trap:** used tracks indices; unmark after recursion **Mutation:** Allow duplicate values but require unique permutations. -> Sorted Duplicate-Aware Backtracking: Equal siblings need a used-predecessor guard. |
| **Backtracking / Combinatorial DFS -> Sorted duplicate-aware permutations** | **Family skeleton:** choose -> mutate path/ownership -> recurse -> restore exactly what this frame changed -> try next choice | |
| [Permutations II](../../src/main/java/org/chijai/day11/backtracking/session1/Permutations.java) | **Skeleton:** Skip used indices and skip i > 0 && nums[i] == nums[i - 1] && !used[i - 1]; otherwise choose, recurse, and undo. **Contract:** used[i] owns an index on the current path; after sorting, equal values are tried in a fixed same-depth order. | **Trap:** duplicate skip requires !used[i - 1] **Mutation:** Ask only for the count of distinct permutations. -> Frequency Math / DP: Multiset factorial counts replace path generation. |
| **Stack / Monotonic Stack -> Stack** | **Family skeleton:** push unresolved state -> current token resolves/combines top state -> pop in LIFO order -> leave only still-pending state | |
| [Valid Parentheses](../../src/main/java/org/chijai/day5/stack/session3/ValidParentheses.java) | **Skeleton:** Push opening brackets; on closing, fail if stack empty or top is not its matching opener. **Contract:** The stack contains unmatched opening brackets in nesting order. A closing bracket must match and consume the most recent opening bracket. At the end the stack must be empty. | **Trap:** closing must match most recent opening **Mutation:** Allow one wildcard that can act as open, close, or empty. -> Greedy Range of Open Counts: One deterministic stack meaning is insufficient. |
| [Evaluate Reverse Polish Notation](../../src/main/java/org/chijai/day5/stack/session3/EvalRPN.java) | **Skeleton:** Push numbers; on operator pop b then a, compute a op b, push result. **Contract:** Postfix expression evaluates when each operator consumes the latest two operands from a stack. | **Trap:** for subtraction/division pop right operand first, then left operand **Mutation:** Change postfix input to infix with parentheses. -> Operator/Operand Stacks: Precedence and deferred operators must now be represented. |
| **Stack / Monotonic Stack -> Monotonic stack** | **Family skeleton:** scan current -> while current resolves stack top, pop and finalize that old item -> push current as unresolved | |
| [Daily Temperatures](../../src/main/java/org/chijai/day5/stack/session1/monotonic/DailyTemperatures.java) | **Skeleton:** While current temp is warmer than stack top, pop index and set answer to current - popped. **Contract:** The stack stores indices whose next warmer day is unresolved, with temperatures decreasing from bottom to top. Current temperature resolves all colder stack-top days, and answer[old] is currentIndex - old. Push current only after resolving. | **Trap:** store indices; answer is i - oldIndex **Mutation:** Ask for the warmest future temperature, not the nearest warmer day. -> Suffix Maximum: Nearest unresolved ownership is no longer required. |
| [Next Greater Element II](../../src/main/java/org/chijai/day5/stack/session1/monotonic/NextGreaterElement.java) | **Skeleton:** For i in 0..2n-1, resolve stack with nums[i % n], push i only during first pass. **Contract:** Loop twice over the circular array while a decreasing stack waits for next greater values. | **Trap:** scan 2n indices but push unresolved indices only during the first pass **Mutation:** Remove circular wraparound. -> Monotonic Stack: The second simulated pass disappears. |
| [Next Greater Element I](../../src/main/java/org/chijai/day5/stack/session1/monotonic/NextGreaterElement.java) | **Skeleton:** Scan nums2, pop smaller values and map them to current, then lookup each nums1 value. **Contract:** Precompute next greater for nums2 with a decreasing stack, then answer nums1 by map lookup. | **Trap:** map answers while resolving the monotonic stack; source values must satisfy uniqueness assumptions **Mutation:** Allow duplicate values in nums2 and query by index. -> Monotonic Stack on Indices: Value-to-answer mapping is no longer unique. |
| **Stack / Monotonic Stack -> Min-heap size K** | **Family skeleton:** push unresolved state -> current token resolves/combines top state -> pop in LIFO order -> leave only still-pending state | |
| [Sliding Window Maximum](../../src/main/java/org/chijai/day7/session1/heap/KthLargestInStream.java) | **Skeleton:** Drop out-of-window front, pop smaller/equal from back, push index, read front after first window. **Contract:** The deque stores indices inside the current window, and their values decrease from front to back. Expire front indices with index <= right - k, remove dominated back indices while nums[back] <= nums[right], then add right. Emit only when right >= k - 1, writing to right - k + 1. | **Trap:** expire front <= right-k; emit at right >= k-1 **Mutation:** Ask for the median of every window. -> Two Heaps / Ordered Multiset: A monotonic deque preserves only extrema. |
| **Stack / Monotonic Stack -> Stack / expression parsing** | **Family skeleton:** push unresolved state -> current token resolves/combines top state -> pop in LIFO order -> leave only still-pending state | |
| [Basic Calculator](../../src/main/java/org/chijai/day5/stack/session3/BasicCalculator.java) | **Skeleton:** Track result, sign, number; on '(' push result/sign and reset; on ')' fold into previous context. **Contract:** Use sign and stack to preserve the expression value before each parenthesis. | **Trap:** apply the pending sign before reset; restore outer result/sign after a closing parenthesis **Mutation:** Add multiplication and division precedence. -> Two-Stack / Precedence Parser: Sign-only deferred state is no longer sufficient. |
| **Stack / Monotonic Stack -> Monotonic stack pop-time boundaries** | **Family skeleton:** scan current -> while current resolves stack top, pop and finalize that old item -> push current as unresolved | |
| [Largest Rectangle](../../src/main/java/org/chijai/day5/stack/session1/monotonic/LargestRectangle.java) | **Skeleton:** Scan through a final height-0 sentinel; while currentHeight < height[top], pop h, use current i as right boundary and new top as left boundary, width = right - left - 1. **Contract:** The increasing stack holds bar indices whose maximal right boundary is not known; a lower current bar closes every taller pending rectangle. | **Trap:** flush with height-0 sentinel; width = right - left - 1 **Mutation:** Ask for the maximum rectangle in a binary matrix. -> Row Histograms + Monotonic Stack: Each matrix row becomes one histogram instance. |
| **Stack / Monotonic Stack -> Stack design** | **Family skeleton:** push unresolved state -> current token resolves/combines top state -> pop in LIFO order -> leave only still-pending state | |
| [Min Stack](../../src/main/java/org/chijai/day5/stack/session2/MinStackDesign.java) | **Skeleton:** Push value and min(value,currentMin); pop both together; getMin reads min top. **Contract:** Store the current minimum with each push, or keep a second stack of minimums. | **Trap:** push duplicate minima and pop auxiliary minimum in lockstep **Mutation:** Add popMax that removes the topmost maximum. -> Doubly Linked List + Ordered Map: Per-depth extrema cannot delete a non-top node. |
| [Design a Stack With Increment Operation](../../src/main/java/org/chijai/day5/stack/session2/MinStackDesign.java) | **Skeleton:** Keep stack plus inc array; on pop carry inc[i] to inc[i-1] and return value + inc[i]. **Contract:** Lazy increment stores pending additions at the boundary index instead of touching k items. | **Trap:** increment the bottom min(k,size) elements; propagate lazy increments when popping **Mutation:** Allow range increments on arbitrary stack indices. -> Fenwick/Segment Tree: One lazy bottom-k boundary cannot represent general ranges. |
| **Stack / Monotonic Stack -> Stack/queue design** | **Family skeleton:** push unresolved state -> current token resolves/combines top state -> pop in LIFO order -> leave only still-pending state | |
| [Implement Queue Using Stacks](../../src/main/java/org/chijai/day5/stack/session2/StackQueue.java) | **Skeleton:** push -> in.push; pop/peek -> if out empty move all in to out, then read out. **Contract:** Use input stack for pushes and output stack for pops; transfer only when output is empty. | **Trap:** transfer input to output only when output is empty; otherwise FIFO order breaks **Mutation:** Require worst-case O(1) dequeue instead of amortized O(1). -> Real-Time Queue / Incremental Transfer: Bulk transfer latency must be spread across operations. |
| [Implement Stack Using Queues](../../src/main/java/org/chijai/day5/stack/session2/StackQueue.java) | **Skeleton:** Offer x, then rotate size-1 older elements behind it; pop removes queue front. **Contract:** After each push, rotate the queue so the newest element is at the front. | **Trap:** rotate the queue according to one consistent push- or pop-heavy invariant **Mutation:** Make push O(1) and allow pop O(n). -> Pop-Heavy Queue Simulation: Rotation moves from push to pop. |
| **Trie -> Trie** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Implement Trie (Prefix Tree)](../../src/main/java/org/chijai/day10/session1/trie/TriePrefix.java) | **Skeleton:** For insert/search/startsWith, walk chars through children; create on insert, fail on missing child. **Contract:** Each node represents the prefix formed by the path from root. insert creates missing child nodes and marks only the final node as a full word. search requires terminal true; startsWith does not. | **Trap:** search requires terminal true; startsWith requires only the path **Mutation:** Remove prefix queries and keep only exact membership. -> HashSet: Shared prefix nodes no longer serve the workload. |
| [Design Add and Search Words Data Structure](../../src/main/java/org/chijai/day10/session1/trie/TriePrefix.java) | **Skeleton:** DFS over trie and word index; on '.', try every child, otherwise follow the matching child. **Contract:** Trie search branches only on '.', otherwise it follows exactly one child. | **Trap:** a dot branches over every existing child; success still requires terminal at the end **Mutation:** Remove wildcard dots. -> Plain Trie: Search follows one deterministic path. |
| **Trie -> Trie + backtracking** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Word Search II](../../src/main/java/org/chijai/day10/session1/trie/WordSearchII.java) | **Skeleton:** Build trie, DFS board paths, stop when prefix missing, collect terminal words, mark cells in-place. **Contract:** Trie prunes dictionary prefixes while board DFS chooses, marks, explores, and unmarks cells. | **Trap:** restore board state, emit each word once, and prune only exhausted trie branches **Mutation:** Reduce the dictionary to one word. -> Board Backtracking: Trie prefix sharing is unnecessary. |
| **Trie -> Binary trie / bit** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Maximum XOR of Two Numbers in an Array](../../src/main/java/org/chijai/day10/session1/trie/MaximumXOR.java) | **Skeleton:** Insert numbers by bits, then for each number walk preferred opposite bits and update max. **Contract:** Binary trie chooses the opposite bit greedily to maximize each XOR bit from high to low. | **Trap:** walk from the highest supported bit and prefer the opposite bit only when it exists **Mutation:** Ask for a proof-focused O(n) prefix-set solution. -> HashSet Prefix Greedy: Testing candidate XOR prefixes proves whether the bit can be set. |
| [Maximum XOR With an Element From Array](../../src/main/java/org/chijai/day10/session1/trie/MaximumXOR.java) | **Skeleton:** Sort nums and queries by mi, insert nums <= mi, answer each query by opposite-bit trie walk. **Contract:** Offline sort queries by limit; insert eligible numbers into a bitwise trie before maximizing XOR. | **Trap:** insert only nums <= query limit; return -1 when the eligible trie is empty **Mutation:** Remove the per-query limit. -> Plain Bitwise Trie: Every number is always eligible for trie lookup. |
| [Maximum Genetic Difference Query](../../src/main/java/org/chijai/day10/session1/trie/MaximumXOR.java) | **Skeleton:** On entering node insert value, answer attached queries, DFS children, then remove value. **Contract:** DFS the tree while the current root-to-node path is stored in a bitwise trie. | **Trap:** the trie must contain only current ancestors; remove the node on DFS exit **Mutation:** Ask maximum XOR with any node value in the whole tree. -> Plain Bitwise Trie: Global trie membership is valid. |
| [Count Pairs With XOR in a Range](../../src/main/java/org/chijai/day10/session1/trie/MaximumXOR.java) | **Skeleton:** For each num, add countLessThan(high+1) - countLessThan(low), then insert num. **Contract:** Count pairs with XOR < bound using bitwise trie prefixes, then subtract low from high+1. | **Trap:** compute countLess(high + 1) - countLess(low) with consistent bit bounds **Mutation:** Constrain n to a few hundred. -> Brute Force: Direct pair enumeration is acceptable. |
| **Trie -> Single-branch prefix walk** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Longest Common Prefix](../../src/main/java/org/chijai/day10/session1/trie/TriePrefix.java) | **Skeleton:** Insert all strings, walk the sole child while childCount == 1 && !isWord, append that edge, and stop at branch or terminal. **Contract:** The common prefix continues only while the trie path has exactly one child and the current node is not terminal. | **Trap:** stop at branch or terminal, including one-word prefix **Mutation:** Ask the query repeatedly while words are inserted online. -> Trie: Stored prefix structure amortizes repeated scans. |
| **Trie -> Every-prefix-terminal trie** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Longest Word in Dictionary](../../src/main/java/org/chijai/day10/session1/trie/TrieWordDictionary.java) | **Skeleton:** Insert all words, validate terminal after every consumed character, and choose greatest length with lexicographically smallest tie. **Contract:** A candidate is legal only if every trie node on its path is terminal, meaning every prefix is also a word. | **Trap:** every intermediate node terminal; lexicographically smallest tie **Mutation:** Drop the rule that every prefix must itself be a word. -> Sort / Lexicographic Selection: Terminal-prefix validation disappears. |
| **Trie -> Shortest terminal prefix** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Replace Words](../../src/main/java/org/chijai/day10/session1/trie/TriePrefix.java) | **Skeleton:** Walk characters until a child is missing or terminal is reached; replace only on the first terminal, otherwise keep the original word. **Contract:** While scanning a sentence word, the first terminal trie node is its shortest dictionary root. | **Trap:** stop at first terminal; missing child keeps original word **Mutation:** Ask for the longest matching root instead of the shortest. -> Trie Full-Prefix Walk: Search cannot stop at the first terminal node. |
| **Trie -> Prefix node + lexicographic DFS top 3** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Search Suggestions System](../../src/main/java/org/chijai/day10/session1/trie/TriePrefix.java) | **Skeleton:** Locate each prefix node; DFS children 0..25, append terminal words, backtrack the path, and stop that search at three results. **Contract:** For each typed prefix, suggestions are the first at most three terminal words below that prefix in lexicographic DFS order. | **Trap:** lexicographic child order; cap each prefix at three **Mutation:** Make product popularity update online. -> Trie + Per-Node Heap: Static lexicographic DFS no longer maintains top suggestions. |
| **Trie -> Reversed suffix trie** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Short Encoding of Words](../../src/main/java/org/chijai/day10/session1/trie/TriePrefix.java) | **Skeleton:** Deduplicate words, process longer words first or insert reversed words, and add length + 1 only when the word creates a new terminal leaf contribution. **Contract:** Only words that are not suffixes of a longer encoded word add word.length + 1 characters. | **Trap:** deduplicate; count only words not covered as suffixes **Mutation:** Ask for shared prefixes instead of shared suffixes. -> Forward Trie: Reversal is no longer needed. |
| **Trie -> Prefix aggregate with overwrite delta** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Map Sum Pairs](../../src/main/java/org/chijai/day10/session1/trie/TrieWordDictionary.java) | **Skeleton:** Compute delta = newValue - oldValue, store the new key value, add delta along root and every key edge, and return the reached prefix node's sum. **Contract:** node.sum is the total current value of every key passing through that prefix; updating an existing key changes each prefix by delta only. | **Trap:** overwrite uses delta = new - old, not full new value **Mutation:** Remove prefix-sum queries and keep only exact key lookup. -> HashMap: Trie aggregates and overwrite deltas become unnecessary. |
| **Trie -> Trie / ranking** | **Family skeleton:** consume one character per edge -> create/follow child -> keep terminal separate from prefix existence -> branch only when the query permits it | |
| [Hotel Reviews](../../src/main/java/org/chijai/day10/session1/trie/HotelReviews.java) | **Skeleton:** Normalize review words, count keyword hits, aggregate per hotel, sort by score and id. **Contract:** Use trie or keyword set to count good words per review, then rank hotels by score. | **Trap:** tokenize consistently, count each required occurrence rule, and apply ranking ties deterministically **Mutation:** Ask for top hotels after every incoming review. -> Trie/Hash Counts + Heap: Ranking must be maintained online. |
| **Intervals / Sorting Greedy -> Intervals / sorting** | **Family skeleton:** order intervals by the boundary that makes the next decision local -> compare current with active boundary -> merge/accept/reject -> update boundary ownership | |
| [Minimum Number of Arrows to Burst Balloons](../../src/main/java/org/chijai/day1/Arrays/session4/Intervals/IntervalGreedyByEnd.java) | **Skeleton:** Sort by end, keep currentArrowEnd, increment arrows when next.start > currentArrowEnd. **Contract:** Sort balloons by end and shoot the current arrow at the earliest ending balloon's end. Any balloon starting <= arrowEnd is already covered. A new arrow is needed only when start > arrowEnd. | **Trap:** new arrow only when start > arrowEnd **Mutation:** Ask for the maximum number of simultaneously active balloons. -> Sweep Line: Endpoint event balance replaces minimum stabbing selection. |
| **Intervals / Sorting Greedy -> Intervals / merge** | **Family skeleton:** order intervals by the boundary that makes the next decision local -> compare current with active boundary -> merge/accept/reject -> update boundary ownership | |
| [Meeting Rooms](../../src/main/java/org/chijai/day1/Arrays/session4/Intervals/IntervalSortByStart.java) | **Skeleton:** Sort by start, scan adjacent intervals, return false if current.start < previous.end. **Contract:** After sorting intervals by start, any overlap with the previous end means a conflict. | **Trap:** sort by start and reject when current start < previous end; equality is reusable **Mutation:** Ask for minimum meeting rooms. -> Heap: Earliest finishing active meeting determines room reuse. |
| **Intervals / Sorting Greedy -> Insert into sorted non-overlapping intervals** | **Family skeleton:** order intervals by the boundary that makes the next decision local -> compare current with active boundary -> merge/accept/reject -> update boundary ownership | |
| [Insert Interval](../../src/main/java/org/chijai/day1/Arrays/session4/Intervals/IntervalSortByStart.java) | **Skeleton:** Emit intervals ending < new.start, merge while current.start <= new.end, emit the merged interval once, then append the untouched suffix. **Contract:** newInterval is the not-yet-emitted merged interval; existing intervals are already sorted and non-overlapping. | **Trap:** closed intervals overlap on equality; emit new interval once **Mutation:** Insert many intervals online with overlap queries. -> Ordered Interval Tree: One linear insertion is repeated too often. |
| **Intervals / Sorting Greedy -> Merge sorted intervals** | **Family skeleton:** order intervals by the boundary that makes the next decision local -> compare current with active boundary -> merge/accept/reject -> update boundary ownership | |
| [Merge Intervals](../../src/main/java/org/chijai/day1/Arrays/session4/Intervals/IntervalSortByStart.java) | **Skeleton:** If current.start <= activeEnd, extend activeEnd = max(activeEnd, current.end); otherwise flush active and replace it with current; flush once after the loop. **Contract:** activeStart..activeEnd is the union of every sorted interval not yet flushed to output. | **Trap:** merge on start <= activeEnd; flush final active interval **Mutation:** Ask only whether any overlap exists. -> Sort + Adjacent Check: Building merged output is unnecessary. |
| **Intervals / Sorting Greedy -> Greedy earliest finish** | **Family skeleton:** order intervals by the boundary that makes the next decision local -> compare current with active boundary -> merge/accept/reject -> update boundary ownership | |
| [Non Overlapping Intervals](../../src/main/java/org/chijai/day1/Arrays/session4/Intervals/IntervalGreedyByEnd.java) | **Skeleton:** Sort by end; keep current when current.start >= lastFinish and move lastFinish to current.end, otherwise remove it; answer is n - kept. **Contract:** lastFinish is the end of the last kept interval; earliest finish leaves maximal room for every future interval. | **Trap:** touching is compatible: start >= lastFinish **Mutation:** Add a profit to each interval and maximize retained profit. -> Weighted Interval DP: Earliest finish can discard a more valuable schedule. |
| **Intervals / Sorting Greedy -> Greedy last-occurrence boundary** | **Family skeleton:** sort by proof boundary -> compare current interval with committed boundary -> merge/select/reject -> move boundary only when ownership changes | |
| [Partition Labels](../../src/main/java/org/chijai/day10/session2/CountUniqueChars.java) | **Skeleton:** Sort by start/end, then merge/count/select with one pass or heap. **Contract:** Close a partition only when the current index reaches the farthest last occurrence of all chars seen so far. | **Trap:** extend the active end to every seen character's last index; cut only when i == end **Mutation:** Allow each character to appear in at most two partitions. -> Dynamic Programming: The last-occurrence forced boundary is no longer mandatory. |
| **Intervals / Sorting Greedy -> DP / patience sorting** | **Family skeleton:** sort by proof boundary -> compare current interval with committed boundary -> merge/select/reject -> move boundary only when ownership changes | |
| [Maximum Length of Pair Chain](../../src/main/java/org/chijai/day9/dp/session2/LIS.java) | **Skeleton:** Sort by pair[1], keep currentEnd, count pair when pair[0] > currentEnd. **Contract:** Sort pairs by end and take the next pair whose start is after the current end. | **Trap:** sort by end and accept only when next start > last end under the strict pair rule **Mutation:** Add profit to every pair and ask maximum profit chain. -> Dynamic Programming: Choosing fewer intervals can earn more, so choose/skip DP is needed. |
| **Union Find / DSU -> Email ownership + DSU grouping** | **Family skeleton:** find canonical roots -> if different, union roots and update size/rank -> if equal, connection already exists -> group by final root | |
| [Accounts Merge](../../src/main/java/org/chijai/day8/graph/session3/AccountsMerge.java) | **Skeleton:** Union account roots on repeated emails, then find each account root, collect unique emails under that root, sort them, and prefix the representative name. **Contract:** emailToFirstAccount owns the first account index for each email; a repeated email proves those account indices belong to one DSU component. | **Trap:** merge by shared email, not equal name; group by find(root) **Mutation:** Ask whether two accounts are connected under online email additions. -> Dynamic Union Find: Roots answer incremental connectivity without rebuilding groups. |
| **Union Find / DSU -> DSU cycle detection** | **Family skeleton:** find canonical roots -> if different, union roots and update size/rank -> if equal, connection already exists -> group by final root | |
| [Redundant Connection](../../src/main/java/org/chijai/day8/graph/session2/GraphBipartite.java) | **Skeleton:** Initialize one parent per 1-based node; for each edge union roots, and return the edge whose roots were already equal. **Contract:** DSU represents the forest formed by accepted edges; an edge is redundant exactly when both endpoints already have the same root. | **Trap:** 1-based DSU; union roots, not raw endpoints **Mutation:** Direct every edge and ask for the extra edge in a rooted graph. -> Directed Indegree + DSU: Two-parent conflicts must be handled before cycle detection. |
| **Greedy -> Greedy / DP states** | **Family skeleton:** name the safe local choice -> prove dominated alternatives can be discarded -> commit it -> update the boundary summarizing all prior choices | |
| [Best Time to Buy and Sell Stock](../../src/main/java/org/chijai/day1/Arrays/session3/StockSeries1.java) | **Skeleton:** For each price, update minPrice, then best = max(best, price - minPrice). **Contract:** Track the lowest price so far; today's profit is price minus that minimum. | **Trap:** update profit before/with a running minimum; buy must precede sell **Mutation:** Allow at most k transactions or add cooldown/fee. -> Dynamic Programming: Hold/cash or transaction-layer state becomes necessary. |
| [Best Time to Buy and Sell Stock II](../../src/main/java/org/chijai/day1/Arrays/session3/StockSeries1.java) | **Skeleton:** For i from 1, add prices[i] - prices[i-1] whenever the difference is positive. **Contract:** Unlimited transactions means every positive day-to-day increase can be harvested. | **Trap:** take only positive adjacent rises; do not double-count a constrained transaction model **Mutation:** Add cooldown, fee, or at most k transactions. -> Dynamic Programming: State must remember hold/cash/cooldown or remaining transactions. |
| **Greedy -> Greedy** | **Family skeleton:** name the safe local choice -> prove dominated alternatives can be discarded -> commit it -> update the boundary summarizing all prior choices | |
| [Gas Station](../../src/main/java/org/chijai/day9/dp/session1/GasStation.java) | **Skeleton:** Track totalNet, tank, and start; when tank < 0 set start = i + 1 and reset tank. **Contract:** tank is the net gas from the current candidate start through the current station. If tank becomes negative, no station inside that failed segment can be a valid start, so the next index becomes the candidate and tank resets to 0. total gas minus cost decides whether any solution exists. | **Trap:** reset start after tank < 0 **Mutation:** Ask for maximum profit route with rewards/costs and optional skips. -> Dynamic Programming: Choose/skip states become real and repeated. |
| [Jump Game](../../src/main/java/org/chijai/day9/dp/session1/GasStation.java) | **Skeleton:** Scan i, fail if i > reach, otherwise reach = max(reach, i + nums[i]). **Contract:** farthest is the farthest index reachable using positions processed so far. If the current index is ever greater than farthest, it is unreachable and the answer is false. Otherwise update farthest with i + nums[i] and succeed once farthest reaches the last index. | **Trap:** fail when i > farthest **Mutation:** Ask for the minimum number of jumps. -> Dynamic Programming: Layered greedy/BFS-style ranges or DP must distinguish jump counts. |
| **Design Data Structures -> HashMap + doubly linked list** | **Family skeleton:** state operation contract -> choose state that makes it cheap -> mutate every index consistently -> enforce capacity/expiry/failure behavior | |
| [First Unique Number](../../src/main/java/org/chijai/day4/LinkedList/session3/LruCache.java) | **Skeleton:** On add update count and queue/list, while front count > 1 pop it. **Contract:** Queue/list stores arrival order; counts decide whether the front is still unique. | **Trap:** counts and candidate order must agree after every add; skip newly duplicated front values **Mutation:** Ask only whether a value is unique at the end. -> HashMap/HashSet: Frequency count is enough when order is not queried online. |
| **Design Data Structures -> LLD / URL shortener** | **Family skeleton:** state operation contract -> choose state that makes it cheap -> mutate every index consistently -> enforce capacity/expiry/failure behavior | |
| [Encode And Decode Tinyurl](../../src/main/java/org/chijai/design/lld/DesignUrlShortner.java) | **Skeleton:** Generate/increment key, store key->longUrl, return domain/key; decode extracts key and reads map. **Contract:** Encode creates a stable short key mapped to the original URL; decode is a map lookup. | **Trap:** generated keys must be collision-safe; decode unknown keys according to the API contract **Mutation:** Ask for plain put/get by key. -> HashMap/HashSet: The design collapses to direct map state. |
| **Design Data Structures -> LLD / domain modeling** | **Family skeleton:** state operation contract -> choose state that makes it cheap -> mutate every index consistently -> enforce capacity/expiry/failure behavior | |
| [Design Fraud Pattern Detection](../../src/main/java/org/chijai/design/lld/DesignFraudPatternDetection.java) | **Skeleton:** Index recent events by account/card/merchant, evict expired entries, evaluate rules on insert. **Contract:** Define which transaction events are retained and which rule/window makes a pattern fraudulent. | **Trap:** key events by the correct entity and apply thresholds inside the intended time window **Mutation:** Ask for plain put/get by key. -> HashMap/HashSet: The design collapses to direct map state. |
| **Design Data Structures -> LLD/API integration** | **Family skeleton:** state operation contract -> choose state that makes it cheap -> mutate every index consistently -> enforce capacity/expiry/failure behavior | |
| [Api Integration Example](../../src/main/java/org/chijai/design/lld/ApiIntegrationExample.java) | **Skeleton:** Wrap client call with typed DTOs, timeout/retry policy, status handling, and clear failure result. **Contract:** Model request, response, retry, timeout, and idempotency boundaries explicitly. | **Trap:** separate transport failure from domain failure; do not partially commit dependent state **Mutation:** Ask for plain put/get by key. -> HashMap/HashSet: The design collapses to direct map state. |
| **Design Data Structures -> LLD / data structures** | **Family skeleton:** state operation contract -> choose state that makes it cheap -> mutate every index consistently -> enforce capacity/expiry/failure behavior | |
| [Design Redis](../../src/main/java/org/chijai/design/lld/DesignRedis.java) | **Skeleton:** Store value plus expireAt, check expiry on get/set, and maintain cleanup or eviction structure. **Contract:** Key-value operations need storage, expiry metadata, and eviction/cleanup policy. | **Trap:** keep value, expiry, and eviction/index state consistent for every mutation **Mutation:** Ask for plain put/get by key. -> HashMap/HashSet: The design collapses to direct map state. |
| **Design Data Structures -> LLD / rate limiting** | **Family skeleton:** state operation contract -> choose state that makes it cheap -> mutate every index consistently -> enforce capacity/expiry/failure behavior | |
| [Design Token Bucket Rate Limiter](../../src/main/java/org/chijai/design/lld/DesignTokenBucketRateLimiter.java) | **Skeleton:** Per key, compute tokens = min(capacity, tokens + elapsed*rate), allow if tokens >= cost. **Contract:** A bucket refills by elapsed time and each request consumes one token if available. | **Trap:** refill from elapsed time, cap at capacity, then consume atomically **Mutation:** Ask for plain put/get by key. -> HashMap/HashSet: The design collapses to direct map state. |
| **Math / Bit / String -> KMP string matching** | **Family skeleton:** name carry/border/bit/contribution state -> update from the next symbol/value -> preserve exact arithmetic boundary -> emit final state | |
| [Find The Index Of The First Occurrence In A String](../../src/main/java/org/chijai/day7/session2/KmpPatterns.java) | **Skeleton:** Build LPS for needle, scan haystack with i/j, and fallback j = lps[j - 1] on mismatch. **Contract:** KMP reuses the longest proper prefix that is also a suffix after a mismatch. | **Trap:** on mismatch fall back with lps[j - 1] without moving the text index backward **Mutation:** Search many needles against one fixed text. -> Suffix Array / Text Index: Rebuilding one KMP prefix table per query repeats work. |
| [Repeated Substring Pattern](../../src/main/java/org/chijai/day7/session2/KmpPatterns.java) | **Skeleton:** Let len = lps[n - 1]; return len > 0 and n % (n - len) == 0. **Contract:** A repeated pattern exists when the final LPS leaves a block length that divides n. | **Trap:** require nonzero final LPS and n % (n - lps[n - 1]) == 0 **Mutation:** Allow one mismatching character in the repetitions. -> DP / Approximate Matching: Exact LPS periodicity no longer proves validity. |
| [Shortest Palindrome](../../src/main/java/org/chijai/day7/session2/KmpPatterns.java) | **Skeleton:** Compute LPS on combined string, reverse suffix from lps length, prepend it to s. **Contract:** Find the longest palindromic prefix, then prepend the reverse of the remaining suffix. | **Trap:** compute the longest palindromic prefix, not suffix; use an unambiguous separator **Mutation:** Allow appending characters at either end. -> Interval DP / Two-Sided Matching: Longest palindromic prefix alone no longer determines the optimum. |
| **Math / Bit / String -> KMP / rolling hash** | **Family skeleton:** name carry/border/bit/contribution state -> update from the next symbol/value -> preserve exact arithmetic boundary -> emit final state | |
| [Longest Happy Prefix](../../src/main/java/org/chijai/day7/session2/LongestHappyPrefix.java) | **Skeleton:** Build LPS over the string and return substring(0, lps[n - 1]). **Contract:** The answer is the final LPS value: longest proper prefix that is also suffix. | **Trap:** the border must be proper; return exactly the final LPS length **Mutation:** Ask for every border length, not only the longest. -> LPS Border Chain: Repeatedly following lps[len - 1] enumerates all borders. |
| **Math / Bit / String -> Bit/string addition** | **Family skeleton:** name carry/border/bit/contribution state -> update from the next symbol/value -> preserve exact arithmetic boundary -> emit final state | |
| [Add Binary](../../src/main/java/org/chijai/day10/session2/AddBinary.java) | **Skeleton:** Use i,j,carry; append (sum % 2), update carry=sum/2, reverse result. **Contract:** Add bits from right to left with carry, exactly like decimal addition. | **Trap:** continue while either input or carry remains; prepend/collect bits in the correct order **Mutation:** Receive a long stream of binary additions to one accumulator. -> Mutable Bit Buffer: Rebuilding immutable result strings repeats carry work and allocation. |
| **Math / Bit / String -> Math / sieve** | **Family skeleton:** name carry/border/bit/contribution state -> update from the next symbol/value -> preserve exact arithmetic boundary -> emit final state | |
| [Count Primes](../../src/main/java/org/chijai/day10/session2/CountPrimes.java) | **Skeleton:** Boolean isComposite; for p*p < n, mark multiples p*p, p*p+p, ...; count unmarked. **Contract:** Sieve marks multiples of each discovered prime starting at p*p. | **Trap:** mark composites from p * p and count primes strictly less than n **Mutation:** Ask primality for isolated very large numbers instead of all values below n. -> Miller-Rabin / Trial Division: Sieve storage over the full range is no longer appropriate. |
| **Math / Bit / String -> Contribution counting** | **Family skeleton:** name carry/border/bit/contribution state -> update from the next symbol/value -> preserve exact arithmetic boundary -> emit final state | |
| [Count Unique Characters of All Substrings of a Given String](../../src/main/java/org/chijai/day10/session2/CountUniqueChars.java) | **Skeleton:** Record previous and next positions for each occurrence, sum leftGap * rightGap contributions. **Contract:** Each character occurrence contributes by distance to the previous same char times distance to the next one. | **Trap:** use previous and next equal positions; flush each character's final contribution **Mutation:** Ask the unique-character count for many explicit substring ranges. -> Offline/Indexed Range Queries: Whole-string occurrence contribution no longer answers each range directly. |
| **Basics / Implementation -> Matrix boundary traversal** | **Family skeleton:** state required output -> identify repeated work -> keep only the state needed by the next decision -> verify boundaries | |
| [Spiral Matrix](../../src/main/java/org/chijai/day1/Arrays/session1/SpiralMatrix.java) | **Skeleton:** Traverse top row, right col, bottom row if valid, left col if valid; move boundaries inward. **Contract:** Shrink top, bottom, left, and right boundaries after traversing each side. | **Trap:** recheck top <= bottom and left <= right before the reverse-direction passes **Mutation:** Ask for repeated rectangular submatrix sums. -> 2D Prefix Sum: Boundary traversal does not amortize aggregate queries. |
| **Basics / Implementation -> Parsing / edge cases** | **Family skeleton:** state required output -> identify repeated work -> keep only the state needed by the next decision -> verify boundaries | |
| [String To Integer Atoi](../../src/main/java/org/chijai/day3/session3/StringToIntegerAtoi.java) | **Skeleton:** Skip spaces, read optional sign, accumulate digit while checking against INT_MAX limits. **Contract:** Parse sign and digits once, clamping before overflow. | **Trap:** skip spaces, consume one sign, stop at first nondigit, and clamp before overflow **Mutation:** Parse arithmetic expressions with operators and parentheses. -> Stack Parser: A single numeric state no longer represents nested syntax. |